SPEAKER 0
This material has been reproduced and communicated to you by or on behalf of the Australian National University in accordance with section 113P of the Copyright Act 1968. The material in this communication may be subject to copyright under the act. Any further reproduction or communication of this material by you must be consistent with the provisions of the Act. Do not reproduce this material. Do not remove this notice.

SPEAKER 1
OK, so, as promised, Let me record the last uh part of the lecture that I couldn't cover. And just as a recap, we have already seen, this is the, again, the hierarchy of interfaces in the Java collections framework. We have already seen the terrible interface, the collection interface. The collection is a set of objects, right, that can be in arbitrary order. Uh, a set is a set of objects. It's a, it's a, it's a collection of objects where we're gonna have duplicates. Uh, queue and deck are specific, uh, collections where an ordering is imposed first in, first out, and we have also iterators that are objects that allow us to walk through, uh, the elements in a collection, and there is a missing, missing part, right, that we still have to, um. Cover which is the hierarchy of maps as I mentioned earlier, if you see map does not actually. Extend collection, OK. It could extend it, but it doesn't, right? And um that has, has some implications, right? Particular as map does not extend collection, that implies that map does not extend iterable and then we cannot actually extract an iterator out of a map. However, there are alternative ways we can extract iterators and uh we can iterate over the keys and the values or the key value pairs in a map. And uh we are gonna see that uh right now, straight away. OK. So, before starting, let me first introduce a couple of concepts. First concept is the concept of Uh, mapping, OK. The concept of a mapping and the concept of a map, OK? These are different concepts. So a mapping can be defined as a relation between inputs and outputs that associates a unique output to each input, and by unique we mean one and only one. So we have inputs. Right? And we have outputs and we have an associate association among inputs and outputs. And there are actually in reality on real world, there are many, many different examples of mapping. A continuous mathematical functions like the ones that you see in, um, let's say calculus course, right? Um, single variable course, right? You have uh Mathematical function like this, right? And then for each input, right? For each input, which is each different value of the x axis, right? You are gonna have a different value in the y axis, right? This would be an example of a continuous mapping, right? Also look up indices, for example, mapping between words and page numbers in a book, right? So in the, in, in the books, right, there is typically a section where you have an index of different terms. Different concepts and for each concepts there is a list of page numbers, right? where this concept has been introduced or has been defined or it is being used, right? But also for example in a contact list, right? In a contact list you have names and you have phone numbers so there is a mapping among names which would be strings and phone numbers and even a list, right? And a, in a list. We can interpret a list as a mapping because we have the elements, right? I don't know, A, B, C, and we have the indices of the elements either 1 and 2, and it's also a mapping that given the index identifier, then what I do is I obtain the element associated to that index, right? So lists can also be considered as mapping between consecutive integers and list elements, OK. So, mapping is a very general concept. It's a mathematical concept, but a map is like a sort of a specialisation of that concept of mapping. And in which sense? In the sense that the correspondence among the inputs and outputs is explicitly stored, OK? So there's a typo here. Among inputs and outputs is splicity store, so a, a map is a mapping, so we have a correspondence among inputs and outputs, but the defining property of a map is that the correspondence among inputs and outputs is explicitly stored within the data structure. OK? Inputs are called keys, right? And the outputs are called values, OK? So these are two very important concepts in a map keys. And values. So we store correspondence among keys and values. Each key is associated to only and only one value. OK? Recall that maps are mapping, so a key cannot have two different outputs. A key cannot be associated to two different outputs. And if a given key is associated to a value, we refer to these two, so to the key and the associated value as the key value pair, OK? And say that maps store key value pairs and you know if you come with a background in other primary languages. Um, uh, maps are as old as dictionaries, associative arrays, or symbol tables, OK? OK. So, we have here the hierarchy of interfaces, right, uh, associated to map and the hierarchy of class implementations, right? So, here we have abstractions. Instructions and in particular in the leaves, right? In the leaves. In the leaves of this tree, right? We have the, let's say, ADT implementations, right? Implementations, right? And you can see, right, that Map both map, right? Map, actually any map here, right? That we have here. is parameterized by two type parameters, right? So, here we have two type parameters. So, K is the type parameter for the keys. Imagine, OK? And V is the type parameter for the values, OK? So if I have a mapping from strings to strings, imagine that they want to store a mapping that establishes the correspondence among countries and capitals. So K would be a string and V would be a string. Um, Australia, Gambara, Spain, Madrid, Paris, France, Brazil, uh, whatever, right? Uh, establish the correspondence among keys and bodies, right? Or if I have, uh, I don't know, uh, mapping among the strings and 3 sets, I would have map, right? A string. And then 3 sets of a string, for example, 3 set. stream, right? So, the first type parameter is the key and the second key is the value. And remember that the map is gonna be storing key value pairs. You can see that there are different implementations of uh the concept of a map. We will mostly study these two, right, which are the Haz map and the tree map, right? The other ones are specific implementations that come in handy whenever your map requires specific properties. Uh, we are not gonna cover them in detail. If you're interested, you can take a look at the documentation and the reference tables provided with the, with the slides, OK? So, hazmap and Tremap. A hazmap Is a data structure that stores key value associating associations using hash tables. Remember the concept of a hash table, right? The concept of a hash table is just an array of link or an array of buckets. Then we hash everything to access to the buckets, and then we may have collisions in a linked list and then we have to search, no, uh, along the colli the, the, the bucket to see whether the element is present or not, OK? And the other one, the 3 set, the 3 maps, sorry. is based on the concept of a tree. So, we have the keys of the map sorted using red, red, black, red, black. This is a typo, red black, red-black trees, OK? I will fix it afterwards. OK. So, as I said, there are two general purpose implementations of a map. We have the Hash map and we have the DreamMap, and both classes implement the map, the map KV interface. Again, remember that maps have two type parameters, one parameter, type parameter for the keys, another one for the values. So has map is gonna have the keys. And a dream map uses an ordering on the keys to organise them in a search tree, OK? And a very important thing that you have to be aware is that the values of a map are never hashed nor order or compare, OK? What we hash is the keys, and what we order is the keys. The values are not hashed. We do not apply hash code method to the values. We do not, uh, order the elements in the map, uh, by value either, OK? And as we said, hacking is usually a bit faster, and it is the preferred choice if we don't need to, to visit the keys in Sorted out. OK? So, here we have an example of a little bit of code, right? Where we are defining a hash map. That is gonna be mapping strings. Two employee objects of the class employee, whatever it is, it is defined, OK? So, we can see here that we are defining a a map. This is the class implementation. Plus implementation, right? And this is the interface, right? This is the interface. And we are following, right? The best practise that I introduced on Monday, on the first lecture. Uh, of this week, which is the fact that we have to, to declare, right? The variable where we are gonna hold, right? The The collection as a type interface, right? Because if we do HashMap here, then our code will actually be tight, will be bound to the particular type of a HashMap and we want generality in general, right? And what I'm doing here is I'm creating an employee and then I call the put method of a map. That is gonna, right, it's gonna put, right, the key, is gonna associate the value, hurry to the key. Uh, provided here, right? And this is gonna be, in this case, an employee, uh, UID like your, I don't know, it might be, for example, your ANU UID. So in this staff map, we're gonna be established uh uh uh storing a correspondence among employee identifiers and the information associated to the, to the employee, OK? So, we have the put method is the first very important operation that we have to become familiar with. And the second is the get, right? So the get method. Of a map of the map interface, right? Is something that When we provide the key, right, it is gonna return us the value, OK? OK. So we put, we can insert into the map a new key value pair. We get, we can extract the value associated to a to a a key given the key, OK? OK. And if there is no value associated to the key. That we provide to get then get returns null. OK? So, in this particular example, if it, if, if it turned out, right, that we didn't have an employee inside the map with this identifier, then here we would get, we would get a null uh value for the employee reference because in such a case, the employee identifier wouldn't be stored in the map, OK? OK. So sometimes, and we are gonna see later an example where this comes in handy is very helpful, right? Sometimes, we may have a good default value that can be used for keys that are not present in the map. So there is a method which is called get or default, right, of a map. That what it will do is it will return the value associated to the key. If the key is present, and if it is not present, then it will return zero, OK? So, there is a default value, right? Which is here. This is the default. The default value that they get or default uh method of the map interface will return if the key that we provide the first argument is not present in the map, right? OK. And so, we need to be aware that if we call twice the put method with the same key, then the second value replaced the first one, OK? And this is mainly because a key cannot be associated to two different values, OK? And put returns the previous value associated with its key argument or null otherwise, OK? So here. We are putting, right? A new key, right into the map. So in such a case, we will get null. Here out of the put method because there is no currently key associated to, there's currently no value associated to this key, but imagine that that key was already stored in the map. In such a case, what we would do is we replace this value, but the current existing one in the stored in the map and we would return this value out of the platform put method and this is what we are saying here, OK? Also, we can call the remove method to remove an element with a given key. And the size method returns the number of key value pers that are currently stored in the map, OK? And the easiest way of iterating over the keys and values of, of a map is using the for each method, OK? So, there is a method in the map interface which is called for each. And This is actually not the only way in which we can iterate over the keys and values, and we are gonna be seeing another way of iterating over the keys and values later in the, in the, in the lecture, but this is the easiest one. And what we do is we supply a lambda expression, right? And the land expression has two arguments, the key and the value, OK? That expression is invoked internally by the for each method, right? It's invoked for each. Entry in the map, entry by entry, right? In typically in an arbitrary order, if there is no order implied in your map data structure. So here, for example, this is the, right, this would be the land expression, right? Sorry. This would be the lambda expression here, right? This would be the land expression, right? To the land expression, we are applying two arguments, right? The first is the key, the second is the value, and then we print the key and the value on the screen, right? And this is just an example, no, an example, uh, of using, uh, of basic, of the basic operations of a map, OK? So what we do here is We declare, let's say an instance of, of the, of the class implementation has map, OK. So, an instance of this guy here, an instance of this guy here or an instance of this guy that we have here, right? And then, we hold, right? As always, this is the best practise that I have recommended. We hold the haz map in a variable of interface time for generality and extensibility in mind, OK? Then what we do is we call the put method 4 times to insert into the map 4 different key value pairs. And the key is gonna be the name of the character or the name of the person and the value is gonna be the date of birth, right? So, this is why we have a string, right? K is the value, local date is the, sorry, string is the key. And local day is the value, right? The value, OK? Then what we do is we print the birthdays, right? And then we got removed, right? We go removed, and then we use the key, right? The key to rem to decide which is the element that we are gonna remove out of the, of the map, out of the, out of the map, right? We can also call put, right? But the difference among this put and these four puts, right, is that The key value pair that we are trying to introduce here is such that the key already exists in the map, right? Because we have already inserted here, right? So in such a case, what we will be doing is we will be changing, right? We will be changing the birth date of Ada Lovelas from this one, right, to this one. We will update it, right? We will modify it, right? We will replace the value associated to the key, right? We can get, right, the, the new, right? The new value of Ada Loli using the method and finally, what we do is we print. Uh, all the key value pairs, right, in a seemingly random order using a lambda, no, lambda expression, right? Lambda, um. Expression, no. With two arguments, right? The keys and the values, OK? And here, there are some questions, right? That for you. Uh, that, as I said during the lecture, these are the sort of questions that you should ask yourself when you are reading gold, right? So, the first question is, OK, I have a hazma, right? Then If I replace Haz map by tree map, so I go here and then I just put tree map here. Would the code compile or not? And assuming that it compiles. Would the code necessarily generate on a screen here, right? Here and here. Would it represent, would it present on a screen, right? No, not here, sorry. For here and here, right? Would it present on a screen, right? The key value pairs in the same order? Or not? What do you think? Also, which is the return value of disput method calls and which is the return value of disput method calls, right? That's the kind of questions that you have to regularly be asking yourself when reading goal, OK? OK. So, let's talk about updating map entries, right? We have seen a little bit of updating map entries here. For example, here, we are updating the value associated to a key. But it turns out that a tricky part when dealing with maps is updating an entry, no? So normally what we do is we get the old value associated with a key, we update it and put it back to the updated value, OK? That's what we typically do or we can do. We get the old value. With a key, we update the current value and then we put it back. However, we still need to handle the special case of the first occurrence of a key, right? What happens if we want to update. The value associated to a key, but there is not currently. Key value per associated to that, that key. And a prototypical example is, for example, uh, using a map. Where we want to count how often words. Occur in a sentence, no? So, for example, if I have, I don't know, I have hello world. Uh, world, uh, Alberto, right? So what I want to extract out of this is a mapping among words and integers such that hello is associated to one because it only appears once in the sentence. We want world that appears 2 times because it's 2 times in the sentence and finally we have uh another key which is Alberto, which is gonna be 1 because it only occurs once in the sentence. That's more or less the idea, no. And one would be tempted to do, to write something like this, you know? So, what we do is, OK, I get. The current code. So imagine that we are in a loop, right? And we are processing all the words in the sentence and then for each of the words in a sentence I'm, I'm gonna try to execute this piece of code, right? So this piece of code, what is gonna do is gonna get the value, the frequency, right? The count associated to the current word is gonna increment it by one and then it's gonna. Assign the new value, the incremented value to the word, to the word key, right? So key. And value, right? However, the problem with this is that it does not work properly the first time a war is encountered. Why? Because if the, if, if we call it get, right? If we call get, no, so this actually doesn't work because if we call get with a word, right, that is not in the map, we are gonna get null, right? And what happens if we get nude, right? If we get nut, we will get. Um A null pointer reception, right? OK. So, a simple remedy to this issue is to use the get or default method, OK? So we have the get or default method that given a map is gonna return the value associated to a key. If the key exists, if the key value perexists and zero otherwise. But there is also another approach. Um Is to call another method which is called put if absent, OK? So it's time. That we are gonna, which by the way, is a little bit dirty, it's not ideal, but it's time that we are gonna get a word, the value associated to a word from the dictionary or from the, from the hash map. What we can do is we can put a value at default value if the word is absent, right? And then we are sure that we will succeed, but the problem with this approach is that we will be, let's say, putting all the time or trying to put all the time, right, a value zero to the world even if the word is already present. So we introduce some overhead. So this is like sort of, let's say a smell, it's, it, it does not, it's not actually ideal, it's not clean, right? A much better alternative is to use the merge method. OK. So there is a merge method, method in the map interface, right? Which simplifies this common operation, and the idea is that we provide. The key, right? We provide. The key, we provide a value. And we provide a function, OK? So, this can be, for example, a land asperion. And this function is assumed to take two arguments. One of these arguments is gonna be The It's gonna be the um current value, right? The current value associated to the world and the other one is gonna be here, the, the, the, the, the one value, right? And how it is gonna work. It's gonna work in such a way that If the word does not exist in the map, then we are gonna associate the value one to that word. And if it exists, what we're gonna do is we're gonna retrieve the value associated to war and we are gonna combine the value associated to war which is stored inside the map. OK. With the value one through that function. So in this function sum, right? The function sum is a function that takes two arguments, right? A and B. One, we would be feeding one, right? As the second argument, and then we would extract the value associated to that word, right? Inside the dictionary, inside the map, and then we would be putting here. Let's say as a second argument, right? You will be putting the value. Associated City toward. In the map, OK. Uh, apart from providing here a sum, we, we could also define like a lambda expression. Something that we could do here is to write something like this, right? AV, no, then R row A plus B, and that would be semantically, let's say equivalent to providing the zoom static method from the integer class, OK? So now, consider another common situation. We want to associate a set with each key, right? So, that is a value. The value associated to a key in the map is in turn asset. OK. So, we're gonna have, as you can see here, a map, right? A map. Which is gonna map and stream. Like I don't know, the world, I don't know, in the assignment book, right? In the book assignment, we're gonna have a book index in which we're gonna have, I don't know, the concept of map, no? And the concept of map appears in lines uh 1 and sorry, in pages 13, 10, and 72 of the book, right? So that would be, uh, the possibility. We have the string here, we have the set, right? With the page numbers associated to that. Book this, right? And What we do or what we can do is to cleverly update the set associated to a given term, to a given word and by using what is called the compute if I sent method of a map, OK? So compute if a set is another method of a map and the idea is. If the term is present in the index, OK? So imagine that we call it with map, OK, we get this set here. Then it just returns the set associated to the term, OK? So in that sense, That would be equivalent to get, OK? So if the term is present, we call get and we return the set associated to the term. Perfect. But if it is not present, then what it's gonna do is gonna call the lambda expression that we provide as the second argument, OK? And the land expression It's actually defined, yeah, the land expression in this case, right? is defined, this is the land aspiration, OK? This is the lamb aspiration. So the land aspiration is defined here, right? This is the lambda expression, OK? So the land expression is assumed to get the key, right? The term, and then it has to return uh new value, right? So, in this case, what we do is we return a new set. Right, and you said. That we will associate, right? To the term and then no matter whether the term is present or not, right, we will be able to add a page number to the uh to the set associated to the term, OK? So we have here a map of strings which are gonna be words in a book to sets that are gonna store the pages in the book where there is a reference to that word, right? And then at some point I need to add, let's say a new number. To the set of page number associated to a term in the book. If the term Is already present. If the key is already present in the map, we are gonna be returning just the set associated to that term and we can add the page number. Perfect. But if it is not present, then we are gonna be passing the term to the lambda, right? And we're gonna be executing the lambda and the lambda, what it does in this, in this case, it. Uh, ignores the term. It doesn't use it. This is why we have the underscore but creates a new set, so it is safe to call the add method of a set to add a new page number no matter whether the key is present or not, OK? OK. And this is a question to think for you, and this is a typo here. This is the book index, right? Why do we consider a tree map and tree set for the book index and the set of pages associated to a set respectively? Why are we considered a tree map? OK? And in order to answer that, you may want to ask yourself in a book. Book book index do you usually get? Your words Alphabetically ordered or not? And the second question that you may answer yourself is, once I consider the set of pages where a given word appears in the book, are these pages ordered in increasing order or not? And you may find the answer to why we choose here to use 3 map and 3 set for the book index and the set of pages associated to a term in the book index respectively, OK? OK. So, recall that earlier I have mentioned that we can iterate, right, over the entries of a map using the for each method plus a lamb expression that gets two arguments, the key and the value, right? But there are alternative ways we can Uh, iterate over the keys, the values, and the set of key value pairs. To this end, We can extract what is called views of a map, OK? Views of the map. And a view is an object of a class that implements the collection interface or one of its sub-interfaces. OK. So the idea is that we have a map. We extract a view of the map, and this view of the map is not gonna be a map, it's gonna be an object. That implements the collection interface or one of its subinterfaces and using these views of the map, we will be able to extract iterators, iterators over the keys, iterators over the values, iterators over the key value pairs, OK? That's more or less the idea. And there are 3 views we can start, we can start the set of the keys in the map. The collection of values, right, which is not a set. And the set of key value pairs, right? You can see that we can get the, we, we can see that we need a set for the keys, we need a set for the key value pairs, and we need a collection of the values for the values and a question for you to think is why the first and last views are sets, right? OK. In order to extract these views, we can use the following 3 methods of the map interface. We have the key set that returns asset, right? Y Z K. Why is it OK. So why the type parameter of the set is K? I can answer you. It's because the set, right, is a set of the keys and the type of the keys is K in the map KV interface. Again, we have the values method that is gonna return a a view, a collection view of the values, and then we have the entry set method that is gonna return a view over the what is called map.entry objects or map. entry interface which actually provides both the key and the value, OK? So there is, apart from the map interface that you can see here, right? Uh, there is another interface which is actually not depicted here, but I'm gonna depict it now, which is called mm, entry, right? Empty N3 KB, right? And this interface that we have here represents a key value pair in the map, OK? And what we can do is call in the entry set of the map interface. What we can do is to extract asset. Of entry, entries, right? A set of entries, a set of objects of a class that implements the entry interface, right? OK. So using these methods, we can go or we can walk over the Keys of a map Using uh for each right loop, a general for loop where we have extracted the set of keys out of the map and then we use the, the typical loop. By the way, question for you is. Why I'm using here set and not a particular set, right? Because I always want to hold my set. Class implementations using an variable of the type interface for generality in mind, right? OK. If I want to look at both the keys and the values simultaneously, then I extract the entry set, right? Which is the 3rd, the 3rd of these views, right? The 3rd of these views. And then each of the entries is gonna be of the type entry string integer because in this case, I'm associating strings to integral values. So using this interface, the map. entry interface, I can call the get key method to start the key out of the key value pair and the get value to start the value out of the key value pair, OK? And then I can do something with K and B. And something which is very important, and if you are not aware. Uh, make us actually a lot of pain down the road when using map entries is that map entry instances are connected to the map, OK? When we extract a map entry out of the map in a loop, right? The map entries are connected to the map. That means that using the entry object, the map. entry object, we can modify. The values. We can modify the map, OK? So, here, there's an example. I extract the set of Map entry objects and then for each key value pair I extract the key, the value, whatever, and then with the entry, I call the set value method. To modify, right? To modify. The To modify the value associated to a key. OK. So you can imagine that I have, I don't know. I imagine that you have the following. A is associated with the number 1, then B is associated to the number 3, right? Then I have C associated to the number 10, right? This is what I have, right? And then I have my iterator, right? Over the first I'm, I'm pointing right before the first, then I move, right, to the next. Right, to the next, and then. When I move to the next, this would represent, let's say the entry, right? The entry. Entry object, right? The entry object with the key and the value. So when I do entry set value, no? V plus 1, what I would be doing is I would go to the map. The map is the whole thing and I would remove 3, right? And store 4, right? So you can see that through this iteration, we can also have the power using the entry to modify the values associated to the keys in the map, OK? That's important for you to be aware because if you, if you are not aware of this and then you start modifying the map, right? You may lead. You, you may not be able to explain some behaviour that your programme is having, OK? OK, so more in regards to map view. And it's actually a couple of questions for you to investigate, OK? And you can actually answer, uh, these questions at home, right? Takeaway exercises, writing on a small code snippet to try to answer them, or you can also look at the tables at the reference tables that I have added here for your reference with respect to the semantics of every of the methods in the map and uh uh the map interface and the class implementations of the. Of the map, right? So HazMap and TreeMap are the class implementations of MAP, and here we have a myriad of different methods of the map interface, OK? But the first question is, as we have seen, right, the key set method of a map, so this method here. Bretans are set. With the keys in the mouth, right? Um, What happens if we add, right, a key to this set using the this method, right? Because the object that key set returns, right, is of, is of interface type set, right? It's of interface type set. And if we go to the hierarchy of interfaces, right? If we go to the hierarchy of interfaces, we can see here, right? We can see here Wait a moment. We can see here, right, that that Set, right? Implements collection, right? And if set implements collection, then there is a method. In the collection interface, right, which is gonna be. Add, right? And that let us us introduce. A new element into the collection, right? So my question for you and for you to think and to test and to investigate is, OK, I extract a set of keys out of the map. A set of keys out of the map using the, the entry set method. The key set method, sorry. What happens if with this, with the set return by this method, I call the app method. OK. And the second is we can extract an iterator out of the object that is returned by key set. Why? Because set implements collection and collection implements iterable, right? But the iterator interface has a method which is called remove. So what happens if we could remove? For an iterator that we have, we have, what happens with respect to the map, if we extract uh Set with the keys of the map, what happens if we call the remove method of the iterator, OK? Of course, we can only call the remove method. Whenever we have walked over at least one element of the set because otherwise, this will generate a run-time exception as we have seen with it. OK. So, these are the, here are the tables with the uh uh most important methods in the interface for your reference and for the most important methods for the Class implementations you can see here, right? That in the H map constructor we can specify the initial capacity of the has table, right? Which is gonna be the number of uh buckets, right? So this would be, this would be the initial capacity, right? Initial. Capacity and there is another parameter that we can specify in the constructor which is gonna be the low factor, right? Which is gonna be Which is gonna be a number between 0 and 1, right? Uh, sorry, not here. Sorry, sorry about that. The low factor, right? So the low factor is gonna be a number which is gonna be uh smaller or equal than 1 and larger or equal than 0 by default. The value is 0.75 and what is still, is telling us is that the hash table, right? Uh, is gonna be rehashed, right? One, the capacity of the array. Uh, is reduced is, is when the array right with the buckets is full beyond 75%, OK? And in that case what we do is we reallocate the array with the buckets in such a way that it is gonna be twice as large and then we rehash the objects and reposition the objects in the new, let's say, um. Uh, rehashing table, OK? OK. So, genetic algorithms, that's gonna be the last block of the lecture, um, Let me first motivate a little bit why genetic algorithms are useful, right? So in, it turns out that the Java collections framework, apart from pro from providing interfaces and providing classes that implement such interfaces also provides what is called genetic algorithms, right? And a genetic algorithms is nothing but an algorithm which is written in terms of a genetic interface, OK? And genetic collection interfaces have a major advantage is that if we use them wisely, so if we know what we are doing. We only need to implement our algorithms once, OK? And this is maybe one of the most important principles of Dynamic run-time polymorphism, right? And uh interfaces and class implementations, interfaces separating abstractions from implementation details. If we are able to write our algorithms in terms of interfaces, the most general possible interface for which our algorithm is gonna be efficient. We will be able to write an algorithm that is gonna work with many different types of collections, not only one, right? And let's illustrate the concept with an example. We want to compute the maximum element in a collection, OK? And depending, right, traditionally, right, in a computing language like in a programming language like C, what we will do is we just write. A version of the code for each of the data structures that I have at hand, right? So these codes are doing exactly the same thing but using let's say different approaches. So, this is the code for array. This is the code for array list, and this is the code for the link list, OK? So, here, what we are doing is we are We are assuming, right, that the largest element is the first element of the array. We are walking over all the indices. We compare The current element, right? With the largest so far and if the current element is larger than the largest so far, we we uh we update the largest variable. OK? Here we are doing exactly the same thing, but as I have an array list, I cannot write in this notation. I have to use the get method for random access, right? And this is an alternative way we could do also this way here with the link list, but we know that. It's not a good idea to use random access with linked list because they are very slow, so we have to use a sort of iterator, right? So you can see that Writing these loops each time is is tedious and error prone, right? Each time that we write the loop, we have to ask. Ourselves and tests, is there enough by one error? Do the loops work correctly for empty containers? What happens with containers with only one element? So it's time that we write each of these loops, we may introduce errors, right? So this approach is error prone is not generally it's a good idea because it, it's gonna introduce a lot of code replication and we want to avoid code replication in our software, right? Uh, this is a principle which is called dry, right? Dry, which stands for don't. Repeat, no, yourself, right? Dry, right? And this is particularly what we are doing here. We are repeating ourselves all the time with different data structures, OK? So we don't want to test and debug this code every time nor implement a whole zoo of methods for each particular data structure such as this one. OK, I have the max method for the array list. I have the max method for the linked list. I have the max method for my doubly linked list. I have the max method for whatever list, OK? We don't want to go down that path, right? So, to solve this problem, We can use the collection interfaces, right? The idea is When I want to tackle a problem like computing the maximum, I think about, OK, I have a hierarchy of interfaces like this one, right? I have a hierarchy of interfaces. And I can declare, right? I have a hierarchy of interfaces like the ones that we have in the collections interface, right? I have collections, right? I have sets, I have queues, I have maps, I have lists, right? So each time that you write a genetic algorithm, you have to ask yourself, which is the most general called interface possible and with more general we mean the one that is upper, upper, no, which is higher in the tree for which. I can implement my algorithm in this case computing the maximum of the elements in a collection in an efficient way, OK, which is the highest in the tree collection for which I can do that. OK, I could write with a list, the maximum, or I could write with a deck or I could write with a queue, but if I do that, I'm restricting, right, the ability to reuse my method with a more general collection, right? So, what we have seen here, looking at the 3 different implementations of the method, right? What we have seen here, looking at the 3 implementations of the method, right? Is that we can solve this problem efficiently using. An iterator, right? So this version which is tailored for a link list also would work with an array list, right? And it would work efficiently. So we can use the Uh, uh, the collection interface, OK, so the most general possible interface for which we can implement the mass method is gonna be a collection which is gonna allow us to efficiently iterate over the elements of, of the collection, right? And this is indeed the collection interface. So what we do is, OK, we write a method and only one method. There's only one method, right? Only one, only one method, right? And we declare the input argument to that method to be collection, right? This is the most general uh interface. So the interface which is highest in the tree for which I can implement the max method efficiently. What I'm doing here, I'm extracting the iterator. I'm extracting the first element, right? Of the iterator and then I iterate, right, over the, over the list, updating the largest if I find one element which is largest, larger than the largest so far, OK? So this piece of code, right, is a genetic algorithm, it's a genetic algorithm. I can use it with a queue. I can use it with a deck. I can use it with a linked list. I can use it with an array list. I can use it with a priority queue. I can use it with whatever implements the collection interface, right? If I now go here and remove this thing. And say, a ray list. OK, I can do that. I can do that, but this is a very bad idea, and this is a very bad idea because this code Max would only work with a red list. It will be very restrictive to the type to which the method can be applied too. So the bottom line is when you write a genetic algorithm, you have to choose the types of the inputs to the algorithm to be the most general possible ones for which using the methods of that interface, like here, I can implement my algorithm in an efficient way, OK? There is a caveat here is that, and that's something that we have also discussed in the lecture, is that arrays are not actually collections, right? Arrays are not collections. So, we need to write a separate method for arrays, OK? That's something that we have to live with. In Java and to this end, there are 2 classes, so these are not interfaces, collections and arrays, right, are 2 classes that provides a medium of static methods with utility methods. The, the methods for sorting, OK, according to uh ordining criterion provided by comparable or comparable interfaces, binary search, which is a very efficient algorithm for searching um entries in a, in an array on, on a list which is assumed to be ordered and some other simple algorithms, OK? So let's explore a little bit. First, algorithms for sorting. We have the sort method in the collections class that sorts a collection that implements the list interface. OK? So, you can see that The most efficient, the most general interface for which I can implement the sorting algorithm is the least interface, OK? It's just one method called sort, we pass a list, right? And then it's gonna be sorting the list in place, OK? Again, why here are we declaring, right, the staff variable of the tape of the tape interface? Why don't we have here a released? OK. Always the same, recurrently the same, OK. OK. In order for this method to work, the sort method, we need that the element type of the elements in the list, right, is of type compare is implements the comparable interface because otherwise we cannot sort because we didn't define a criterion to order the elements. Alternatively, we can pass a comparable object as a second argument. This is what we are doing here. And this is just a, it's a way to generate in place an object that implements the comparator interface. We could have defined here a class, right? Class, my comparator implements a comparator, right? As we did in the previous lecture, in previous lectures, but this is just something, a function, and static function from the comparator. Uh, interface that let us generate a comparator which is gonna be helping us to order the elements by salary, OK? So we have an employee that is a field of the employee which is get salary that returns the salary, and here we are able to generate a comparator object that is gonna help us, right? Sorting the, the elements by ascending salary, OK? We can also call the starting method reversed order, right, and pass it as a comparator to the sort order and then we would be sorting the list in the standing order. And something that we can also do is to call the reversed method of a comparator object, right? The reverse method of a comparator object in order to sort, in this case, the stuff by the standing salary, OK? OK, so binary search. Very quickly because we are uh uh sort of running out of time. Binary search is a very, let's say, interesting algorithm that is used to search an element within a sequence of elements. Whenever we assume that the list is ordered, OK? So the idea is that we start. If we have an array, right? And we want to find an element, no? Whether the element is inside the array, what we do is we start, right? We start in the middle, right? In the middle. So Remember that this thing here is assumed to be sorted, right? So we start with the middle element, right? And we ask ourselves, is the middle larger than element, right? So if the middle is larger than element. Then we know that the element is gonna be in the first half, right? However, if the middle is smaller than element, we know that element is in the upper half. So what we do is, OK, assume that. Middle is larger than element, so we know that it is here. So what we are gonna do in the next step is to find the middle here, right? The middle here. And then we ask the same question. Is uh the new middle, right? Is larger or is it smaller and depending, right, we go to the lower half or the upper half. And then we keep having the iteration, the interval, sorry, at each iteration, right? At each iteration we have, we have, we have, we have until at some point, the interval is just one element. And at that point, we may even, we may be able to find the element or maybe to state that the element is not in the list, OK? So this is actually a much faster algorithm than linear search. So linear search is the one that starts from 012, right, up to M minus 1. So this uh uh halving interval halving algorithm or binary search is much faster and in terms, in the case of, for example, having Uh, 1,024 elements, which is 2 to the power of 10, right? Then we can In order to transform, let's say this interval, right? With 10 2 10 elements into one, right? We need to take 10 steps, right? Because at the first step, right, we are gonna be reducing the interval by 2. This is gonna give us 10 to 9. In the next step, 10 to 8. This is gonna be the size of the intervals till finally we reach an interval of size 2 and then like what? So in 10 steps. We are transforming the originally very large, right interval by reducing it by a factor of 2 at each step. In 10 steps, we reduce it to an interval of size 1. And at this point, for sure that we either have found the element, right, or we haven't, OK? OK. OK, so there is an algorithm which is gonna, a method which is called binary search in the collections class to which we provide an elements, right? An elements list, so the array or the collection and the element that we are looking in the array, right? And what it does is that it tries to find, right, the target within the sequence, right? If it finds the target within the sequence in that's a positive number or a non-negative number. And And if, and the non-negative number is gonna be just the index of the element within the sequence, but if the element is not found, it returns a negative number. But the negative number that it returns carries some information, in particular. If we, if we denote by I the output right of this binary search algorithm. Then using this formula. OK. We can determine the index at which we should inject the element if we wanted to, to keep our collection sorted, OK? So, imagine that we get a -1, then we take, right? We take minus. So imagine that I is equal, sorry. Imagine that we call binary search, right? And then we get that I is equal to -1, OK? So that implies that -1 -1 is gonna be equal to what? It's gonna be equal to 0, right? So this is telling us that we have to insert, right? Target. At indexedo within the elements uh sequence in order to keep the elements uh. Order, OK? And to be actually efficient, and that's something which is very connected to what we have been talking through all the lecture, the input list. Must support efficient random access, OK? Actually we can provide a list like a doubly linked list or a lean list that that does not support efficient random access and it will actually sort this out but this the it not sort sorry that it will perform the binary search, right but then it will be much slower, right, because in order to locate the element in the middle, right. Instead of accessing to the element in the middle just uh randomly in one in one axis, right? It will have to walk through the elements to it find the middle. So all the benefits of the binary search somehow are sort when we use a list that does not support efficient random access, OK? There are many other simple algorithms in the collections class, right? And as a general, let's say, comment, it's actually a good practise to express our computations in general using these simple algorithms, right? Because it actually helps a lot when reading uh your code and also it helps others to read your code. And let me put an example. So for example, look at this piece of code, right? So in this piece of code, we have um List or Actually, we have, um, yeah, a set of words, let's say. No, not a list, sorry, we have a list of words, so it would be something like list stream, right? This string And then we walk over the indices in the, in the list of streams and then for each of the words, uh, for each of the words, we get the word using the index and we ask whether the word is equal to C++, right? And if it is equal to C++, we modify, right? The element associated to the index I to jump. OK, so if I have, I don't know, A. In my list, no, I have A, B, I have uh C++, and I have C++, right? So this code, what it's gonna do, right, it's gonna go to the C++ and D C++, right? And it's gonna replace it by Java, no, here and Java. So what it's doing is it's replacing all the occurrences of the C++, right? By Java, right? But there is a method, right, in the collections class, right, which is called replaceable. You just provide the list of words. To provide C++ and Java, and what it will do is it will replace all the occurrences of C++ by Java. The point is that reading this piece of code, right? It's much easier for the programmer and for yourself to understand what the code is doing rather than reading these kind of things, right? And again, this is a generic algorithm, right? It's a generic algorithm because here we can provide a list, right? Whatever the implementation of the list is, OK? OK. So, the table in the next slides introduced the simple algorithm in the collections class. So you go here. You will see all these algorithms, binary search, uh, you will see here computing the minimum of a collection, computing the maximum copying and filling lists, right? With elements and also method for reversing list swapping. There are many, many different methods. What the best way to get familiarised with this is actually to, to start writing the small code snippets and read the documentation in the tables, the reference tables. To see whether you can fully understand them if you didn't fully understand them to try to. To practise, right? And, and, and get an understanding and finally something which is actually very, very useful is utilities for converting among collections and arrays and that's something that actually uh happens is the fact that OK we have modernised our code uh using collections that's the way to go. Every our algorithms are generic. We work with collections all the way through, but at some point there's a piece of code in our project which is legacy and only needs and only knows how to process arrays. So the only way that we can call these methods in this, uh, legacy code is by transforming, um, our collections into a race back and forth, right? And to this end, there are utility methods. So there is a method which is Of the list interface, right? Which is list. of that given an array, right? This is an array that given an array of strings, let us generate a list of strings. And that is the opposite method that led us, let's say, generate a um an array out of a collection. And extracting an array out of a collection has been traditionally quite tricky in Java, but since Java 11, there was. Uh, very, let's say, um. Lucid and, and very clean solution to this problem which is the so-called two array method in the collections interface to which you can provide a function, right, which is called a generator function, right? Generator function, right? So you can think of a, of this generator function. As a lambda that given the size of the array, right, it's gonna be allocating a new array of the element type of size N, OK? And it turns out that we have seen that in the lambda expression that this notation, right, provides a reference to the new method that explicitly does that, right? But we could also replace this by the lambda function and it will work out of the box, out of the box. So the idea is that The two array method at some point, right? Needs to allocate the array. So you are providing here as an argument, a hint, right? That allows the two array method to know how it has to allocate the array that it will be able to reund afterwards, OK? That's more or less the way. OK, so I'm done, uh, for the lecture of today. Thank you. Thank you very much for all your attention. And if you have any issues or any doubts, please don't hesitate to ask me on that forum or whatever if there is any concept that you didn't uh get or you may need clarifications. Uh, so that's it. Thanks a lot for your attention and see you soon.
