SPEAKER 0
This material has been reproduced and communicated to you by or on behalf of the Australian National University in accordance with section 113P of the Copyright Act 1968. The material in this communication may be subject to copyright under the act. Any further reproduction or communication of this material by you must be consistent with the provisions of the Act. Do not reproduce this material. Do not remove this notice.

SPEAKER 1
OK. So it's already 5:05, so let us start with today's lecture. So my name is Alberto. I'm not Bernardo as you can see. So this week I'm gonna be lecturing uh in the course, uh thank you to Bernardo for inviting me to uh to give this lecture and we are gonna be talking about the, hello, we are gonna be talking about astrac data types and the Java collections framework, OK? These are two main concepts that we are gonna be introducing today. The very important concept of astra data type in computing. And how does Java express as data types using the so-called Java collections framework, OK? And before actually going into the materials, right? I thought that it would be a good exercise that we do a bit of a recap of what we have seen in the last couple of weeks. Uh, because we are gonna actually need it in order to fully understand the um. The concepts that we are gonna be introducing today, OK? So very quick recap of what we have seen in the last couple of 2-3 weeks. OK? So let me maybe open this. Notebook that I have here. OK? So we have seen the concept of interfaces, right? OK? And what, what's an interface? Can anyone tell me what's an interface? It's a contract, yeah, but how is this materialise? It's a list of methods, right? It's a list of method signatures with the inputs, outputs, name of the functions and so on, right? We also have classes, right? We have classes. And we have seen a couple two kind two kinds of classes, right? We have seen concrete classes, right? Concrete classes, and we have seen as well abstract classes, right? So what's the difference among a concrete and an after class? Can anyone tell me?

SPEAKER 2
I'm very close to you.

SPEAKER 1
Yes, so in an abstract class it's like uh it's partially defined, right? It's partially defined and via extension we need to be able, right, or we must implement, right, the components of the abstract class. Right, that are not yet defined. Also, we cannot instantiate abstract classes, right? OK? So it's a matter of let's say putting in a one unit code that can be reused by the extensions, OK? And we know, right that. A class, no? A class er may extend right? Another class, right? This is the is us relationship, right? This is inheritance, right? And we know that at most one, right? So there is no multiple inheritance in Java. A class can extend at most a single class, OK? And we have that. Um Uh, class, right? May implement. Interfaces, right? OK? And 0, let's say 01 or more, OK? So we have these relationships, this relationship among classes, let me. How Let me move this. So we have extension. Right, have extension and we have a class may extend interfaces may implement interfaces, right? And we also have that an interface. May extend. Other interfaces, right? And here we can also have 01, right? Or more, OK? OK? So we have interfaces. A set of methods without typically without their implementations. We have classes. Classes can be classified into concrete classes and abstract classes. Right? And then we have relationships among classes and classes and interfaces. A class may extend another class. A class may implement. Other interfaces and interfaces may extend other interfaces, OK? So these are the sort of relationships that we are working with in object orientation, OK? So we are gonna be today, we are not actually be covering. Uh, new object oriented features we are gonna be covering the Java collections framework, but the Java collections framework actually uses, uh, a lot of these features, OK? So you really need to be familiarised with these features in order to understand what we are gonna be covering today, OK? OK. Apart from concrete and abstract classes, there is a third kind, let's say, which we have talked a little bit about it but not in detail, which is the concept of generic, generic classes, generic classes and generic interfaces. So you remember that Bernardo introduced the concept of a realist, right? And the array list was parameterized, right? So you have the angle brackets, no, and within the angle brackets you can put here a type, right? Integer or you can put here a string, right? Or you can put here whatever, right? Any reference type, OK? So the Java collection framework is mostly based on genetic classes and genetic interfaces, OK? You don't actually need to know much about genetics in order to be able to leverage the features of the Java collection framework, but you need to know at least that a gene genetic class is like a sort of template. Which has a generic type parameter which is a placeholder where you can select the particular type that you want the elements of that class to be, OK? So we can have an array list of integers, we can have an array list of strings, we can have an an array list of employees, we can have an array list of managers, and we only need to define the the template class only once and we instantiate the template. With different values of the type parameter to generate different classes, but we only need to define the class once, OK? And this is actually something which is not available in other programming languages. There is no concept of genetics. And then if you want an array list of integers, you need to define an array list of integers class. If you want an array of strings, you have to define another class, OK? So genetics is a mechanism. Very powerful mechanism that allows us reduce code replication because with a single template definition of a generic class we can instantiate that generic class and generate. Let's say a myriad of different classes, OK? And you will see All the way through during the lecture that all the interfaces and all the class implementations in the Java collection frameworks are generic classes and generic interfaces, OK? And in order to see whether a class definition or an or an interface definition is is parametric, is generic, you will see the angle brackets, OK? Like in a realist angle brackets, and in between the angle brackets we need to specify. The particular type we want our list or we want our genetic type to be instantiated with, OK? OK. So now, hopefully we are all on the same page. OK, so let me start with the concept of, OK, this is the agenda for today. I will give a very short introduction to Astra data types. And then we will move to the Java collection framework, OK? So the Java collection framework is the way that Java, uh, uses in order to represent the abstract concept of an abstract data type, OK? We will see the collection interface and the iterator interfaces which are, I would say. Uh, the most, the most important two collection interfaces in the Java collection framework we will. Uh, do or navigate through the main interfaces that there that is there apart from the collection and iterator ones and then we will explore concrete class implementations of such interfaces in uh in particularly we will see, uh, lists, OK, we will see queues, we will see sets, and we will see maps, OK, class implementations, OK. Uh, and finally we'll see the, uh, availability in the Java collections framework or what is called generic algorithms for algorithms, for example, for sorting lists, uh, or algorithms for copying algorithms for, uh, doing binary searches or different kinds of searches, OK? So in the Java collections framework there are genetic algorithms which are written in terms of interfaces that we can leverage. Like we can reuse using our own class implementations of the interfaces that the Java collection framework provides, OK? OK. So What is an abstract data type? So an abstract data type formally. Is a mathematical model for that data type, OK? We are not gonna actually go down that path in this course, but it's good for you to know that behind everything that we are gonna cover today, there is also a mathematical formalism where you can have propositions, lemmas, and you can prove things about that abstraction, OK? As data lives are defined in terms of behaviour, semantics from the perspective of the user and not in terms of implementation, the specific details, OK? This is why they are abstract and not concrete. So in an abstract data data type, we have to specify the possible values that the data type may have, but not how they are laid. Out or storing memory. We have to specify a set of operations that a user may perform on the data type, but not how they are implemented and we have to specify the behaviour of such operations, OK? How they are defined, how these operations are defined, OK? And I'm gonna be Talking one again and again and again and again about the same concept, OK, which is separation, right? A among abstractions and concretions, separations among abstractions and implementation details. You don't need to know how, let's say a a list, right? is actually implemented under the hood to be able to leverage a list of elements, OK? And that's something that we have been doing, right? You, you use the array list, you use, you use the operations of the array list. But you don't actually need to know how the list is implemented under the hood, how the elements are laid out in memory, how they are related to each other. All the, all these details are somehow removed from the concept of an astra data type, OK? And this is actually a very powerful. Mechanism in software engineering, right? Because we can, one objective that we have in software engineering is to be able to write our code in terms of abstractions, right? The more that we are able to write our code in terms of abstractions, right? The more general, the more extensive our code will be, OK? OK, so IDTs are cornerstone for code reusability and modularity as they allow us to rely on abstractions rather than in concretions or specifics, and there are many, many examples of astral data dykes. We have lists, we have queues, we have decks. We have sets, we have maps, we have trees, we have graphs, many, many different NSA data types, OK? Usually that would be covered in a lot of detail in a data structures course, right? But this is not actually a fully data structure of course so most of these are gonna be covered, let's say. Not from the depth that we would like, but this is what it is, OK? And typically different programming languages offer different mechanisms to specify Atra data types and their implementation, OK? So for example, in C++, there is the so-called standard template library that provides many different implementations of Astra data types. In Java, we have the Java collection framework, OK? OK. So let's illustrate the concept of an extra data type with an example. And the list, OK? So for the purpose of the example, a list is just represents a finite sequence of elements of the same type. We can have a list of strings, a list of dates, a list of employees, and there are two defining properties of a list, OK, as an extra data type. The first one is that. Duplicates, right? are allowed. OK? We can have the same element twice in a list, why not? And the second one, which is actually very important, is that the order of the elements in a list is preserved, OK? So that we can assign right an index. To an integer index to each of the entries of the list, OK? So there is an ordering ordering implied, OK? In the concept of a list, OK? Some of the examples of list operations, although there might be many more. Could be add a new element to the end of a list, remove the first occurrence of an element, uh, from the list given the element. Get the element of the list at at an index that we specify and replace the element of the list at the specified index by the given element. OK? These are examples of operations. So you can see here that I'm specifying the as data type, OK? I'm, I'm defining, OK, the defining properties, how do we conceive the data type. And which kind of operations we can make with the data type, but I'm not saying anything regarding implementation details. There is a separation among abstractions and implementation details, OK? And the user of this data type only needs to know about this information, OK? Doesn't need to know about how we are going to implement in practise these concepts, OK? OK, so this is just a representation, right? Of the different operations, uh, we have here the four operations. We have the list, a list here, right? with the elements -1, 2, -1, and here we have the indices, right? So 01 and 2. You can see here this is the ordering, right? So the list -1, 2 minus 1 is not the same as the list minus 1 minus 12, right? There is an ordering among the elements. So here we just add an element at the end, right? And then we generate a new index, index 3, we remove the element, the first occurrence of the element minus 1, right? So we remove this guy here and then we get a list without that element. We get the element at index one, right? Which is this one here. And then we set Yeah We set the index 2, right? To the element 110. So this is what we are doing, right? We are changing, right? We are changing the value, right? So you can see that we are just representing the sequence, 0123, each of the elements in the list has an associated index. And then we have a set of operations that we can do with such a an abstract data type. But we are not specifying again. Anything regarding how these lists are going to be laid out in memory and how we are going to implement such an operation, OK? Such the different operations that we want to perform, OK? OK. So Of course We need We have the abstraction, but in practise, right? We need to realise the abs the abstraction, otherwise we cannot actually execute a computer programme, right? And the way to realise an abstraction is via an implementation of the abstract data die, OK? So we have abstractions, implementation. What is an implementation? And implementation. So what, what we should, let's say, uh, mm fulfil when we want to implement an Astra data type. We must adhere to the specified behaviour of the Atra data dive and its operations, OK? However, the implementations have the freedom to internally store and lay out in memory the elements differently and thus to implement the ADD operations accordingly to the chosen internal data storage layout, OK? And each implementation typically leads to a different trade-off balance among memory consumption and the computational cost of its operations, OK? So the idea is that, OK. I, I need to decide how I'm gonna implement the, the corresponding as data type and typically I need to achieve a balance, a balance among different factors, OK? So I may consume more memory at the at the expense of uh at the benefit of being able to perform the operation faster or more efficiently or I can reduce the memory consumption, but some of the operations might be, let's say slower, right? So there is always a sort of in the implementation of Astra data types a sort of uh trade-off that you achieve when you take decisions regarding how you are gonna realise or materialise the concept of the Astra data type, OK, how you are gonna realise that concept, OK. And there is a natural question that. We need to get good at. Right? And this is part of the programming enterprise is that Given a problem specification, right? An algorithm and and then I write an algorithm which is let's say the most appropriate data structure, right? That I, I need to use in order for my let's say software to fulfil a set of constraints, a set of requisites, OK? And the answer is that depending on The The particular features of your algorithm, how often it access to data or how often it modifies data, right? There might be some implementations that are best suited than others, OK? And then you have to develop this sort of a skill of say, OK, if I have this algorithm, right, which should be the the the data structure which is best suited. To balance different constraints of my obligation, OK? And we have uh different implementations because we want to actually provide different potential balances among different uh conflicting constraints, OK? OK. So these are examples of implementations of the abstract data type list, OK? So we have what is called the link list. And we have a single link list and a doubly linked list, and we have the array-based list, OK? So in a single link list, what we have is A set of nodes, they are called the squares are called nodes, OK? So this is a note. This is a node as well, right? This is a node. And in the notes, we have references or links, this is why it is called link list, to the next element in the list, OK? So here we have a list with 3 nodes, 10 minus 27, each of the nodes. Has two main properties or two fields. One is the data that we want to store in the note, which is going to be the element of the list, and the other is gonna be a reference, a pointer, right? A reference to the next element, right, in the list. The last element, how do we know whether we have reached the last element? Can anyone answer? Yes, so if, if we have a note such that the next reference points to noon, we know that we, we are in the last element of the list. And apart from that, apart from the notes, right? We also may need or it may come in handy to have a reference to the first element and a reference to the last element, OK? OK, you can see two different pointers there, a reference to the first element on the list and a reference to the second to the last element, OK? So how would you implement the the list in Java? What would you do? Can we discuss a little bit about that? How many classes do you think we are gonna need? Let's start with that. Can we start with the orange boxes? The orange boxes, we are gonna need, we are gonna need a glass for that, right? So the orange box is going to be a class that is going to have two fields, right? One is going to be the element and the other one is going to be a reference to the next element, right? Is that all that we need? May we, for example, may we, for example, define a class singly linked list. That is gonna essentially have two different pointers, one for the to the first element or two references, one to the first element and one to the last element, OK? So that would be a possible way of implementing, not the only one, right, but that would be a natural way of doing. We have, uh, a class, right, to model, let's say all of the components of the list, and we have a, a class, right? That is gonna provide access to the first and the last element of the list, right? And then if we were to implement the operations of the Atra data type, so adding an element or getting an element or extracting an element, we will be writing something. Which is tied to the way that we have decided to lay out the data structure internally, OK? The code that we are gonna write. Is tight is bound right to our uh approach to internally represent the details of the Atra data type, OK. We also may want as an alternative to consider a do-lily list, OK? So in a do-lily list, Apart from a pointer or a reference to the next element, to the next note, I also have a pointer to the previous one. And it wraps around, so the last element points to the first. Right? And the and the. The the the the next pointer of the last element points to the first, right? So we can see that here. And the previous pointer or the previous reference of the first element points to the last, OK? Why one would. Do that instead of uh Using a single link list. Because you may imagine that we are consuming more memory. Because now the nodes, right? They have 3 fields, not 2, so we are consuming more memory, right? But why do you think one would like to store links to the previous and the next? Yes, because it may simplify in terms of efficiency, let's say, it may simplify the implementation of some operations. Not only simplify, but some operations might be implemented in a faster way, OK? OK, maybe it's not easy for you to anticipate that now. OK, but for now, just keep that in mind, which is depending on how do I internally design my data structure, right? I might be able to come up with an implementation of the abstract data type. That is much faster, OK? At the expense usually of having to consume a little bit more of my money, which is exactly what is happening here, OK? OK? More or less. OK. Does it make sense? If not, please interrupt me and then I can, I can go into more detail or whatever, OK? And the second one The 3rd 1, sorry, the 3rd implementation is the array based list, OK? This is indeed the basic, let's say principle of the array list data structure, right? The idea is that we have an array. OK? And in the array we have We have the elements, OK? And typically we have two different parameters, right? One is the size of the list, which is how many elements we are currently storing, so here we have 3 elements, and the second parameter is the capacity of the array which is. Uh, how many elements we can store, right? And for example, consider, I don't know. Removing an element from the array-based list, OK? So imagine that we want to remove the first element. So we want to remove this thing here, right? What do you think we have to do? How do we have to update the array?

SPEAKER 2
Change the pointer The second element Remove the reference That from the first one that was pointing to.

SPEAKER 1
Not, not, not quite. Not quite. And you could do that in other languages but not in Java. OK, I, I, I know where you are going, but here you cannot do that. So the, the, the, the thing is that if we eliminate element 10, right? Then element minus 2 and element 7, right? Are, are now the new indices, right? The new indices of these two elements are 0 and 1. But these are not currently located in 0 and 1. They are currently located in 1 and 2. So what do we have to do in order to properly locate -2 and 7? We have to move them to the left. Right? So we have to take -2, we have to move -2 to the first element, and then we have to take second, right? 7 and move it to the, OK? So you can see that. Imagine that you have a very long array, right? I don't know, you have 1 million elements and you, you eliminate the first one. It's going to be a very costly operation because you have to be moving N minus 1 elements where N is the size of the land of the list. Right? But what happens, for example, If we want to eliminate an element from here, I don't know if we want to eliminate this thing. What do, what would, what would you do?

SPEAKER 2
Thanks to the There you go.

SPEAKER 1
There you go. So what we would do is, we just go, we go to the previous element. And we will change, right? The pointer Such that the pointer of the previous element right? is gonna be pointing to the next of the one that we are removing. And that's it. Right? So you can see that eliminating one element. From the linked list Is, is a very fast operation. However, eliminating an element from the array-based list is very costly operation in general because we have to shift all the elements to the left. Or to the right, depending on whether we remove or add, OK? But what happens with searching? Imagine that someone gives you the index of the element. Index 2 Index 0, index 1, this is called random access, OK? What happens? What happens with the arrive base list? Would you say that it's a costly operation? No, because the only thing that I have to do is square brackets, the index, and then I get it. That's it. But what happens with the link with the link list? You have to scan You start from the first element and then you move to the next, to the next, to the next, to the next, as many times as the index. So you can see that both implementations, the linked list and the array list achieve a different trade-off. The array list. Aris list is very fast in providing elements using the index. But very slow in removing or adding elements. However, the link list. Is very fast in adding or removing elements in the middle but is very slow in searching for an element given the index of the element in the list, OK? OK. Hopefully It is making some sense. OK. So we have seen the concept of astral data type and the concept of the implementation of an astral data type and now we are gonna start talking about how do we uh realise that concepts in in Java, OK? So astral data types in Java and the way to do it is the Java collection framework. So Java offers different programming constructs that can be used to express uh as the data types and their implementations. And Among these, a natural choice, right, is to use para parametrized or genetic interfaces to specify ASTA data types and parameterized classes implementing such interfaces for ATA data type implementations, OK? So There are actually different ways that we would use because something that we would do is I use an abstract class for the concept of abstract data die and then extension. For an implementation of the ATA data type that could be potentially a way that we would conceive or express this separation among abstractions and implementations, but this is not the, the, the way that the Java collection frameworks uses. The way that it uses is that it uses parametric or parameterized or generic interfaces for astra data types and then classes that implement such interfaces for the actual concept of implementation, OK. So parametrized interfaces and classes are also known as generic, right? And that's something that I have uh already been told before, no? They are denote by angle brackets. So whenever we have the angle brackets like here, right? There is a placeholder type parameter. E is is typically used. E is the letter that is typically used in the documentation, although we could use T or D or P or R. It doesn't matter. It's like a placeholder. It's something that you can replace by an actual reference type integer, string, employee, customer, whatever, OK. OK, and this is actually a warning, right? And we could actually define our own interfaces. For the concept of a list or for the concept of a queue or for the concept of a map or a set, we could define our own interfaces in our programme, right? We could do that, but it's generally a good idea, right? To stick to the ones in the Java collection framework. And this is because if we do this, we wouldn't be reinventing the wheel, right? And we will also allow, right, our class implementations to reuse genetic code which is available in the Java collection framework, OK? So imagine that in the Java collections framework, there is an algorithm for sorting, right? And then this sorting algorithm relies on the interface list which represents the ATA data type list, OK? If I now instead of defining my own interface for a list, implement the one available in the Java collection framework. Then I'm gonna be able to reuse the algorithms that are there. With my own class implementation without having again to reinvent the wheel. I could sort, right? My lists. Without having to redefine, to reimplement the sorting algorithm, OK? Sorting algorithms, there are many different sorting algorithms. Right? Sorting is something that I may need to do, but I don't want necessarily to spend time in something that for sure that many other people have worked for a long time and they are gonna be able to do much better than I do, right? So that's more or less the point, OK? Uh, the point is If there is, let's say a data structure, right? Uh, an interface in the Java collection framework that satisfies the requirements of your software, do not reinvent the wheel, use what is available there already, OK? So in the rest of the lecture what we are gonna do is we are gonna be exploring the basics of the Java collection framework, how to put it to work and discuss on some of the design decisions. And actually a note, right, is that There will be uh more details regarding parameterized definitions and genetic. Genetics for interfaces classes and subtyping in the later in the course. So Bernardo actually next week, right? Or in 2 weeks' time, he will give a lecture on Uh, more detailed lecture on genetics, although we need to know the very basics of genetics in order to leverage the Java collection framework, OK. OK. OK, so let's illustrate. The main principle. Of the Java collections framework. Using an example, right? How can I What is one. I don't see it It's OK. I cannot say it's not, it's not there. This year Yeah, can you come? Help me. Please This year. It's not there. OK, it's OK. Let's let's survive with this. OK. So let's illustrate the concept, the main principles in the Java collections framework with an example. An example is gonna be a queue, right? And the queue is just another example of an ASTA data die, OK? So as mentioned earlier, the Java collections framework separate separates interfaces and implementations, OK? Um, so a queue interface, there is an interface which specifies that the elements can be added at the tail end of the queue and removed at the head of the queue, OK? So a queue is nothing but a data structure, right? that reflects. The The real world concept of a queue, right? So if you go to the cinema and you want to buy a ticket, you need to wait in the queue, right? Till you are served, OK? So exactly the same concept here. And what we what we have to handle there is. The let's say the first element of the queue and the last element of the queue. The first element of the queue is typically called the head of the queue, right? And the last element of the queue is called the tail of the queue, right? And then what we do is we add elements to the tail of the queue, right? So we have 3 people waiting, then I, I got to the tail and then I have to wait for the other 3, right? And then the one which is in front of the queue is the one which is gonna be served, OK? So we can only add elements at the tail, sorry, at at the tail and we remove elements from the head, OK? So A queue preserves what is called FIFO order, OK? So FIFO order is first in, first out, OK? And this, let's say somehow captures The order in which the elements right are stored internally, right? and how the elements are extracted from the queue and how how they are removed, OK? So FIFO is actually a policy as well for schedulers and so on, OK? Again here. The only thing that we are exposing is that there is an order in the elements, right? So the 1st element, the 2nd, the 3rd, the 4th, that there is the concept of a tail and the concept of the head, right? But we are not exposing anything, uh uh uh regarding how we are gonna actually implement that data structure, OK? So what we can do is we can represent that with an interface. OK? So an interface may look like, like this. So I have an interface which I call queue. Which is parameterized by the element type, OK? And then the element type, in this case, I call it E, although I would have called it FRTS, whatever, it's just a placeholder name, OK? Whenever I actually use this template, I will need to specify what actually E is, OK? By actually replacing the placeholder by an actual data type, OK? And then I have 3 methods. Add, remove, and size, OK? So add, given an element, right? I'm gonna add the element in the tail end of the queue, remove. I'm gonna remove the element at the head of the queue and size is gonna return how many elements that are in the queue, OK? So as expected, the interface does not expose any details on how theQ is implemented, nor how it is constructed. Do you see here that there is no constructor. I'm not specifying how I'm going to construct, how I'm going to build the queue. I'm not specifying anything. Because if I would do, right? I may need some parameters which are actually depending right on the implementation, OK? So the the the interface exposes the operations of the astral data type. This is what we are doing, OK? And in the Java collections framework, there are 2 possible implementations of the Que interface. The right deck, OK? And the link list. These are the two possible implementations of the Que interface. The con the array deck, I'm gonna talk a little bit about it now, it's called, it's based on the concept of a circular array, OK? While the linked list uses a linked list and that's something that we have already seen, right? So a linked list is nothing but a set of nodes where we have links among the different nodes, right? And that helps us in very efficiently rem uh removing or adding elements, OK? OK. So this is how. The, the right deck definition may look like. So I have a class, right? A class. Which is gonna implement the interface, correct? It's gonna implement the interface. And then here I'm gonna specify as the fields of the class, right? And the implementations, the implementation of the different methods in the interface, OK? Apart from the constructor, you see? So here I have the class constructor when I specify how many elements at most I'm able to store in my array, OK? And then I have the uh methods of the class that are gonna override the corresponding ones in the interface, right? Remember that I have these methods in the interface and I have here the class methods right? that are gonna implement the one of the interface, OK? So this, this actually, this actual implementation is based on the concept of a circular array, OK? So in a circular array, what I have is Right, in an array I can assign an identifier, right? 67 and apart from that I have a couple of pointers which are gonna be just integer identifiers. So here first will be pointing to 2 and then last will be pointing to 7 and what I'm gonna be doing is that whenever I want to introduce an element. Uh, in the tail end of the queue, I will be updating the last pointer, right? However, whenever I want to extract an element from the head of the queue, I will be updating the first pointer of the queue. OK, but at some point it may happen that the array gets full, right? And there are different uh options, right? If it gets full, I can just make it larger, right? Or I can just generate an exception or whatever. OK, so for example, in a circular array, which operations would you, would you do if you want to extract the element in the head of the queue. What would you do? So what, what is the head of the queue right now? Where is this pointing? First is the head, and last is the tail. OK? So where is the head right now pointing? To which index Index 2, OK? So there are two things here. One is the index of the element within the array and the other thing is the actual element, OK? The actual element is what we have inside the boxes and the index is the number that I have drawn on the right, OK? So at which index is currently the head pointing to? 2, right? So what would you do to extract the element in the head of the queue? Extract the element, just get it. What would you do? You just, yes, you just go to array, right? and access to the element in position number 2. And what happens if not only you want to to to get it but that extract it from the queue? What would you do? What would you do? What would you do with the first, with the first pointer? Well This is the queue, right? This is the queue, right? This is the first element. This is the last element. If I extract the first element. I remove it, which is gonna be. The head OK, so what I have to do if I want to remove an element? Here. With first. OK, you have to update first, right? But there are two ways we can update it. We can, we can decrement one or we can increment one. What do you think we have to do? Increment Are you sure? OK, if you increment it. Where is the head gonna be pointing to? OK, so if you increment it, you are gonna be pointing here, right? Am I correct? And this is, this is, this is the element too. I mean, look at here, right? What I have done? I have removed the head, right? Where is the head pointing here? OK. OK, so do you still believe that we have to increment the first pointer? Or decremented Decrement, right? OK? Yes, decrement. There you go. This is what we have to do. To remove the, the head, we have to decrement. Right? You have to decrement the first. OK. But at some point, right, we will decrement it assume that we want to keep extracting elements from the, from the queue. We will decrement it again, we will decrement it again, but what happens at some point? What happens at some point? At some point we are gonna reach here, no? And then we're stuck 3. What do we have to do at that point? We cannot access element minus 1, right? What do we have to do? Imagine that my first pointer is pointing to index number 0, right? And I want to extract from the queue element 3. I would decrement first. That will lead to the index -1, but -1 is not actually a position in the circular array. So what do I do what do I have to do with that pointer? Yes, shift it to the highest index. This is why it is called circular array, because when you achieve one of the limits, right, and you determine, you have to wrap around, you see. OK. So imagine that this is again, no, this is the head, right?

SPEAKER 3
Maybe, maybe, maybe if you. A uh element.

SPEAKER 1
OK? So what would you do to add an element? So remove element we have said, OK, we decrement the pointer, we reach one of the limits and then we wrap around. What would you do to add an element? 0 Elements are are added at the tail, right? So imagine that I'm in this situation, right? And I want to element, add element 5, right? What I would do is Right, because this is what a queue is, right? So, in a queue, right? Imagine that this is the, the cinema, right? And all the people are waiting for get the ticket to the cinema, right? If I want to add a new element and there are 4 people waiting, I have 2 queue at the at the end of the, right? OK, so again. Let's assume that I want to add an element. In this circular array, what would I do? Would I let's let's make the question uh more specific would I decrement. The tail pointer or the last pointer or would I increment it? Increment Decrement, yes, I would decrement it. So what I would do is I would move, right? The last point there. By the agreement in it by 1 and then I would add element 5 here, right? It's clear? More or less? Does it make sense? Of course at some point if you keep adding elements to the tail you will reach the head. And at that point you are full, right? You cannot still add more elements and again you should decide what I'm gonna do. I'm gonna raise an exception and the user cannot keep adding elements or I may generate a new array and copy the elements to the new array having more space. OK? But you see, right, that the operations of adding elements to the tail or extracting elements from the head are very different for a circular array if we compare them to the operations that we will have to do, for example, if we implement the queue with a linked list, because with a linked list, what we are going to be doing. How do you add A new element at the tail of the queue. Well, What would you do? I would add a new node, I will generate a new node with the new keyword, right? Then I will put it here. 5. I will update last. Right? Well, first, I would do last, next, will point to new, right? And then I will update the last element. So in in the circular array I decrement the pointer. And then insert right the element into the new uh hole that I have created. In the second one I create a new node using the new keyword in Java to create a new instance of the node class and then I update the next parameter of the last and finally I move the last to a new element and I would have injected right. A new element at the tail of the queue. OK? What would you do? To add to extract the first element. To start the head, what would you do? Yes. So the the only thing that we would do is we change the first, right? Here. That's it. And what do you think is gonna happen with the first element? So once we have updated the first pointer, what is gonna happen with the first element? Are there any references pointing to it? Are there any arrows pointing to it? What is gonna happen with it? It's gonna stay there forever? Yes, so as there are no longer references to it, then the, the garbage, garbage collector at some point will enter. And we'll get rid of it, OK? While in the circular array case, right? We have the array And the references will be there, right? Sort of difference situation. OK? I'm very slow. OK, so We have the queue interface and we have two different implementations of the queue, right? Interface and then class implements queue. So how do we use this in practise? In practise, Um What we want Is that when we use a queue in our programme, Um, OK. Let me see OK, OK. So we want to write our programme in such a way that it doesn't matter which kind of cue we have used, OK? Our programme should work with both the circular array and with the linked list, OK? If we start writing our programme such that the programme depends on whether we are using a circular array on a linked list, we are not going in the right direction, OK? So how do we achieve that? How do we achieve that? The idea is that the only point at which we should use the class implementation. Is whenever we create the the instance of the object. And then we get the instance of the object that we have created into a reference type of type interface because remember that variables of type interface. Where can I declare variables of type interface? Can I do that in Java? I'm talking about declaring declaring a variable of type interface. I'm not declaring out, I'm not talking about instantiating an interface. It's, it's a different thing. So what what I can do with an interface in Java? I can declare variables of type interface, I can declare arguments to functions of type interface, and I can also specify the return type of a function or a method to be of an interface type. So what I'm gonna do is in my programme, I'm gonna declare a variable of type interface queue and then I'm gonna assign that variable to the. Object of the class that implements such an interface and then once I have done that. The rest of the programme is gonna be written in terms of the interface. Why, why, why do you think I'm doing this? Or why do you think this is a best practise to do so? Imagine that I have written a programme that manages, let's say a scheduling of jobs or or clients that uh access to the service or whatever, right? If I, if I have written my programme in terms of the queue interface and I have followed that advice, meaning that I'm gonna just refer to the class implementation of the interface in a single line in particular when creating the instance of the class implementation. Then I would be able to remove, to change the class implementation circular array biling list in a single point of my programme and the rest of the programme will work out of the box without having to touch a single line. Because it will be written in terms of the interface of the queue. OK. Does it make sense? OK. OK, there are other reasons why we we want to use variables of interface type, and one of the reasons is that typically the class implementations of the interfaces like a, a right deck or link list have methods, may have methods that are totally unrelated to with the interface that we are implementing, OK? So for example, a linked list, you may imagine that a linked list apart from implementing the queue interface also implements the list interface, OK? And the list interface has methods that the queue interface doesn't have because in the queue we cannot inject elements in the middle of the queue, right? So if we start using implementations in our programme all the way through instead of interfaces, we may end up calling methods that are specific to the implementation. And that's problematic because we actually want to use the concept of a queue in our programme, not the concept of a linked list in its in its full, let's say flavour, OK? OK. So, again, there is, there is a question, right? Which is which of the two implementations of the Que might work best in practise, right? Um, the circular array is more memory efficient. However, it, it has finite capacity, as we have mentioned before, and needs to be reallocated whenever it fills up, right? And the answer of which implementation is best in practise depends on each specific use case on, on the algorithm that we are writing, right? But evidence shows that the array deck generally outperforms a little leaks because the former has less impact on the garbage collector, and that's something that we have discussed as well, right? Whenever we We start right injecting elements or removing elements then from time to time the garbage collector has to get in into the memory and remove the memory that we are living, that that doesn't actually happen with the circular array, right? And the fact that the garbage collector has to be under the under the in the background, right? removing elements that that are let's say uh forgotten in memory. Introduces some overhead in the execution of the programme, OK, while the circular array typically you don't have to it doesn't suffer from it, OK. Just, just a comment.

SPEAKER 3
So just to give an example of two different scenarios, right? One could be like imagine I am selling products and then I'm storing the products I sold somewhere. I don't want a right. Right, because I want to sell more and more, and that's it. So I would put like everything there. Now, I have a car park. I have a limited number of spots, right? So should I use a limited list to represent that

SPEAKER 2
one because that will never change, right?

SPEAKER 3
So these are the different cases and like, well, memory efficient is just if I actually misuse the data structure, right? If I use something else to represent that. Uh, list of products I sold, right? So I have to all the time like, uh, reallocate, reallocate. So depending on the, the language, so like in C language you would have to have like this memory allocation that's called malok or ko, and then you have to do the allocation, location, location. So that's very, very efficient. So choosing well that uh is is the best thing right like not really only about oh this is a memory efficient and then I apply this to scenario it's more about like uh what scenario happened and then what. Efficient for that scenario.

SPEAKER 1
Yes, thank you, thank you, Bernardo. OK, so if we follow the best practise that I have mentioned, right? And with the best practise we mean that we are going to be referencing to the class implementation in a single point in the programme whenever we create it. Uh, the, it turns out that, uh, the array deck, which is the circular array, was introduced later in the Java collection framework in Java version 6, and it was already a bunch of code, right, that was already written in terms of the key interface, OK. So thanks to the people, right, that use this thanks to the fact that many people, right, use this best practise of using. The reference to the implementation in a single line of code when creating, right? The the instance replacing let's say the link list implementation by the right deck was just a matter of changing changing a single line. And everything worked out of the box without having to do anything else, OK? So again We need to be able, and that's maybe the the the bottom line of of this lecture, right? And also it's related to what Bernardo has been mentioning in the in the last weeks right? is that we have abstractions because we want to hide implementation details and we want to be able to write code that in terms of abstractions. The more that we are able to depend on abstractions, the more general our code will be if we start writing our code in terms of implementation details. Right? We are not going in the right direction, OK? OK OK, so collection and iterator interfaces. These are two of the most important interfaces in the Java collection framework, OK? Uh, so collection conceptually represents a collection of objects of a given type that are not necessarily arranged in a predefined order. Uh, some sub-interfaces may impose an order of the elements. That's the case of list or queue, but there will be others that don't, OK? So a collection is you can think of a collection as a set of objects of a given type. All the objects have to be of the same type, but there is not necessarily a predefined order in which these elements are arranged. There will be some kind of collections for which this order is there, like list of cues, list and queues as we have seen queues is first in, first out, lists, there is an order, a sequence, an element identifier, but there are other collections like sets where there is not actually any order. In a set of elements, there is no order implied, OK? And there are two main methods in the collection interface. A, right? The a method that adds an element to the collection, whatever that means. It's going to mean different depending on the kind of collection. It's going to mean different things depending on the kind of collection that we have. And it returns a bullion, right? Re returns a bullion. Uh, that is gonna be true, uh, whenever the, uh, collection on which we have called them the a method, uh, changes, OK, because there are cases in which it may happen that we try to add an element and we are not actually modifying the collection. So you can imagine a set, you have a set of elements you cannot have duplicates in a set of elements if you add an element that is there already in the set, we are not gonna be actually be modifying the set, OK? And then the second method which is actually very important is the iterator method, OK? So iterator methods is gonna return an object that belongs to a class that implements the iterator interface, OK? So we have here the first interface is collection. And collection, right, has a method called iterator that returns an object that implements the iterator interface, OK? And the iterator interface is an object. That helps us or allows us to visit the elements of the collection. One by one Again, without exposing, without exposing how the collection is organised internally, OK? You may imagine, OK, I have a collection, I start adding elements, but at some point, my programme will need to access the elements, right? So any collection for any collection no matter which collection we can extract an iterator and using the iterator we can visit the elements of the collection one by one and the iterator interface is gonna provide to us methods that are gonna help us in visiting the elements in the collection, OK? OK, so the iterator has 4 methods. Has next, next removed and for each remaining The idea is that one visits the elements by calling next, OK? Next, next, next, next, next. But typically one needs to be very careful when calling next. Because if we go next, whenever we have already reached the end of of the end of the collection, that is going to raise an exception, OK? So at the beginning we create the iterator. The iterator is pointing, uh, before the first element, and we are gonna see that now. And then we go next, right? And we visit the next, the next element, next, next, next, next, next. But we have to be careful because if the iterator is already at the end of the collection and we go next, that is gonna raise an exception, OK? So to this end there is a method which is called has next. Which is gonna tell us whether the iterator has actually reached the end of the collection or not, OK? If it has reached the end of the collection, Haznetz next will return false. If it doesn't, if, if it hasn't reached the end of the collection, Haznet has next will return. True, because there is still a next element that we can visit, OK? So with this interface. OK? This is how the recipe, right? for accessing, for visiting the elements in a collection looks like, OK? So in the first line, we are just creating the collection. Again, you can see that the variable that we are using is of the type collection interface. OK? So we want always to use To receive class implementations into variables of type interface, OK? And then I have a loop, right? Well, before the loop, I have the iterator call, right? The iterator call where I'm getting the iterator and then while the iterator still has a next element. I extract the next element. Right? Do something with the element and then Iterate through the loop, OK? So this is the, the, the general rec recipe. To uh visiting all the elements in a collection. OK. And something very important is that, as I said before, the collection. Right? Conceptually does not imply necessarily that there is an order of the elements. Depending of of the kind of collection, there might be an implied order of the elements or not, OK? So in a list there is an order, in a queue there is an order, and when we start the theator we are gonna visit the elements of the list and the elements of the queue in the order underlying the collection. But there are, there is, there are, uh, collections for which there is no order and we may access the elements in an arbitrary order. OK, seemingly random, we don't know, we cannot assume in our programme that we are gonna get such a, such elements in that order for sure we know that we are gonna be visiting all the elements in the collection, right, but not necessarily in a in a specified or predefined order because underlying the connection. The collection, there might not be any order implied, OK? We can also write. Uh A short version of this loop, right? So this is a shorter version of this loop where we use the for each, for each notation, OK? So we use 4 string elements call on call, right? And then we have access to the elements. And under the hood, what Java is going to be doing is gonna be translating the for each loop into the loop that you see you can see at the at the top, OK? So for each loop is like a sort of syntactic sugar to write the Why loop in a much concise, concise way, OK? So you can see that if you define your own implementation of the collection interface. And then you define the iterator method. In that class implementation, immediately, if you have a loop like that, then it will work out of the box with your own collection. And are we exposing, right? Are we exposing with these loops how the collection is internally organised? What do you think? So these loops are gonna work for the link list for the singly link list for the doubly linked list for the array list they are gonna work for the circular array for implementing the deck as well as for the link list for implementing the queue. They are gonna work for the set. So you can see that with the derator, we are actually extracting. The process of visiting the elements in the collection we are not exposing anything about the internal layout. OK? So this is why iterators exists because they let us To provide an abstract mechanism. To walk through or to visit all the elements in the collection without exposing any detail regarding how the collection is internally laid out again the same concept again and again and again separating abstraction from implementation details. OK? Separating interfaces from class implementations. This is the success recipe in object-oriented programming. If you want to go to large scales, if you want to go to large-scade bases. If you do not apply this sort of separation of concerns, abstractions. Implementation details, your code won't be able to scale. OK. And the forage loop actually works with any object that implements the the interface, OK? So there is another. Interface Which has a single method, right? Which is the iterator method, OK? So the iterator, iterator method that we have here actually The collection interface gets it from the IEA interface. OK. So there is another interface at the root, right, which is called iterable that defines the iterator method for collections. And you will be able, let's say to reuse the for each loop, right? For with a collection as far as your collection implements the interface, OK? OK. So this, this actual picture, this picture is actually very important because I believe that it clarifies a lot the dynamics of how iterators works in Java, OK? Because it turns out that there are also iterators in other primary languages but they actually work very differently compared to how they work in Java, OK? So when you think about iterator iterators in Java, iterators in Java, you should think of them about actually pointing within elements, OK? So they are not actually pointing to an element, they are pointing within the elements, OK? When you start. When you create the terator, right? The terator, the iterator is gonna be pointing before, before. The first element And then you go next. Right? When you go next, what what you are gonna do is to walk over the element and return it, and you are gonna be pointing in between the 1st and the 2nd. Till you reach the end, past the end. Right? There are other programming languages, right, in which the iterator is not conceptually pointing between elements, it is actually pointing to the element itself. And then in order to extract the element, you don't need to move forward iterator. The only thing that you need is to get the element. It doesn't happen in Java, OK? Because in Java in order to be able to extract the element we need to walk through it. We have to walk over it. OK. Does it make sense So it's the same logic with the interpreter. With that. Interpreter They are the same log line.

SPEAKER 2
Now check Elements themselves doesn't.

SPEAKER 1
Interpreter, you mean the interpreter and compiler? Well I don't see, I don't see the connection here but um. Yeah. So, OK, it's OK. But do do you understand what I'm saying here? Do you, do you mean like the type or you

SPEAKER 3
mean? Like the compiler iterator are completely different from um this but uh The iterator is the way. Over a collection, right? So I have a list and then I go like this, this, this, these items. Uh, now, of course, this will be related. Compiler in the sense that there'll be some checks to see whether like these elements should belong to that list or like if I ever get anything over uh a value uh list. That's like the highest me. So we have like uh A subtype or a type whatever right like when he says that he, which would be like, let's say I'm creating over a list of Employees, right, then I would say, well, this is this employee or am I trying to add to the list something that does not, is not an employee, right? So that will be checked by the compiler in some ways and uh also like uh during runtime if you are doing this at eding this dynamics. Now, The iterator, uh, it's, it's actually, iterator is a very advanced, uh, concept because When you like you're gonna learn how to actually implement iterator in the next course, but in this course it's just to say that, well, the reason why you can do that more and then you say like uh this is a string of person from people, right? It's it is enabled because of this iterator because we know that the collection we know that there are no.

SPEAKER 2
stop, but we don't know how it works.

SPEAKER 3
I don't know how it works. I just know that I can call next, right? And it will come at some point it will stop and that's it, right? So what, uh, Albert is saying is like, well, if you're thinking about this interface, like, well, it will be like in between because you didn't access the, the elements yet.

SPEAKER 1
Yes. And maybe if you have never seen iterators in other programming languages, it doesn't make much sense, but this is actually sort of quite a lot of confusion from people that come from C++, right? So at the end of the day, the bottom line is that in order to get the element, right, you need to walk over it. If you don't walk over it, you're not gonna get the element. And this is just a design decision behind the interface. The Java the Java designers could have decided to design that interface in another an alternative way, but this is what they decided to do, right? OK. And also there are these methods, right? The for each method of a uh of a collection and then for each remaining uh of the iterator for which you can provide a land expression, OK? So land expression is something that Bernardo introduced. Um, last week, no? So the idea is that you are gonna provide a land expression. The land expression is gonna get the element in the in the iteration, and then you are gonna be able to do something with the with the iteration. For example, if you would like to print all the elements in the collection, you would have a land expression that given the elements, print the element, and then you will give that land expression. To the for each method of the collection interface or to the for each remaining method of the iterator interface and then under the hood right what what is gonna happen is that uh it's the the walkthrough over the elements for each element is gonna be calling the land expression that you are gonna be passing to the functions to the methods, OK. And this is just a um. An exercise for you, right? That. Imagine that you have an object belonging to a class that implements the collection interior interface, the array list. In implements the collection interface and you have been working already with a realists. OK, so there will be, there will be a method of a realist because a realist implements collection. Call forage to which you can pass a land expression that is going to print the elements. And then you another alternative is that you can start an an iterator out of the array list because iterator because array list also implements the collection interface and then pass a land expression to the for each remaining method of the iterator and then the iterator is gonna be calling your land expression for each of the elements in the traversal, OK. OK. So the iterator interface. Also provides a remove method. OK? So the remove method allows us to remove An element out of the collection. OK? But the question is which element are we gonna are we gonna remove when we call remove, OK? And the one that we are gonna remove is the the last one that was returned by next. OK? So again, in order to be able to remove an element out of a collection. With an iterator We need to walk over the element before we can remove it. And the rational, the reason behind that, right? The the intuitive reason behind that is that in order to be able to decide whether you are gonna remove an element, first you need to access the element, right? You need to, uh, I don't know if you want to eliminate the people that are called Alberto from a collection, first you need to walk through a person which is called Alberto to decide whether you're gonna remove it or not, OK? So that implies that if one wants to eliminate an element in a particular position, then one must still skip past the element from the first element, right? So if you want to eliminate with an iterator the element in a position number 4, what would you do? So imagine that your collection is a list, right? List implements extends collection and you have a list and then you want to eliminate the element in position number 4, let's say, what would you do with the iterator? You will create the iterator, you will start a loop. You will walk walk through all the elements till you walk through element with index number 4, and at that point you stop the iteration and call remove, OK? That's maybe not the most efficient way of eliminating an element, right? But this is the way that you will have to do it with an iterator, OK? OK, so here you can see and Example of code that eliminates the first element from a collection and the question for you to think is why the the if statement is needed there. Could they have just put IT.n and either. remove. Or do I actually need the IF statement?

SPEAKER 2
If it's the last gentleman that Yes If yeah, if

SPEAKER 1
it is the last element, it can lead to an error or if the collection is empty, right?

SPEAKER 3
You removed everything and then what you're gonna remove next.

SPEAKER 1
Yes, there you go. OK, so if we would write Just ITE. next and IT. remove. If the collection right? that we want to iterate is initially empty. Then what's next is gonna be false. Because we are already past the end of the struct of the date of the collection because there are no elements. OK, so this is why we need the I statement because otherwise that code could crash, right? That would be a corner case. If our collection has at least one element, we are fine. Without Dave, if our collection is empty, then that code will crash. OK. Indeed, it is, it's also illegal to call remove without a preceding next call. OK? So for each remove, there has to be a next call. And that means that if we want, for example, to eliminate two consecutive numbers, we cannot call remove twice. We need to call next, remove, next, remove, OK? OK So genetic collection utility methods. So the and this is again something that I have already mentioned, but to make it explicit, right? Is that the collection and interface, uh, the collection and interface interfaces are generic. Yeah, that is wrong. This iterator, right? So be iterator And this should be interfaces. Sorry, I will fix it after the lecture. Meaning that you can write utility methods that operate on any kind of collection, right? So here, for example, we could write, let's say a class which we can call Collection utility methods, right? That is gonna provide a set of generic methods that are gonna work with any kind of collection using the collection interface that's something that we could do, right? For example, here we have a piece of code which is a static. static method of the glass which is uh this method here right? Which using the collection interface is gonna determine whether the object provided as the second argument. Belongs to the collection or not? OK. That's it. So what do we do there? We are, we are iterating right over the elements of the collection. If the element of the collection is not null because we may have null elements in the collection, then we check whether the current collection element equals the object that we have gotten at the second argument, and if it equals, the return true. If we have been, if we have walked through all the collection and we didn't find the element, we return false, right? So what is the power of this method? Can anyone tell me what is the power of this method? So imagine that now I remove, I change the type of the parameter call and instead of putting collection, I would list or I would, I would queue or I put Dick Or I put set. Would that method be as powerful or as expressive? No, because we could only pass, right? Objects of classes that implement that particular interface. However, here we are writing a method, right? That gets a collection. So any class implementation that implements a collection. Can be passed in as the first argument. Right? Even for collections that. do not yet exist. So imagine that we write this method, OK? And we leave it there. And in 2 years' time, in my project, I need, I need a new kind of collection. I write my class implementation, I implement the collection interface, and then with that that new class implementation that I have written two years apart when I grow this method. I can reuse that method without without having to touch a line of code. Perhaps if you are more familiarised with dynamic programming languages like Python, that's not a big deal because in Python you can do all this, this stuff. But in compiled languages which are not object-oriented, OK? Like Java, I don't know, like C, for example, is not an object-oriented, it's a procedural programming language. Achieving this is not possible. You cannot write codes that are gonna be able to be reused with new data structures that will be developed in the future. Do, do we Does it make sense what I'm saying? Does it make sense? OK. So, imagine that I do the following thing, OK? I go here and I say, OK, this is gonna be an array list. E OK. What's the problem with this method? The the method is gonna compile and the method is gonna be functional, right? But does this method depend on abstractions on or on implementation details? Don't be shy. I mean, it's OK if we say something wrong. I mean. Yeah, I mean, the method would be. Not as expressive because the only thing that we can pass to this method is an array list. But there are other kinds of lists and there are other kinds of collections. Right? So the point is that when we write methods, let's try to use the most generic possible interfaces when declaring the types of the arguments because this is gonna be these are gonna help us writing code that is gonna be able to be reused with many different class implementations of the interface. You can do that. You can do you can replace their collection by a rail list and that code is gonna compile, but you can only use that code with a rail list. You are restricting a lot. Right? The applicability of this method. And that would amount to write a code that depends on implementation details, right? Because the array list is a class implementation of an interface, right? OK. So it turns out that There are, let's say a family of methods, right? This generic collection utility methods that are so frequent that the developers of the Java collections framework decided to put it into the collection framework, OK? So I'm not gonna get into details, but there are a set of methods of the collection, right? That are generic utility methods that are there because they are so frequent in, in, in, in so in different software that it makes sense to have them there, OK? So for example, this is just a method. That help us transforming an arbitrary collection into an array. Right. And you may, you may wonder why do we need to transfer a collection into an array? Why do you think we may need that? So you, you can imagine that having a generic method that given an element is going to determine whether that element is in the collection or not, is going to be helpful, right? But for what purpose do you believe it might be helpful to have a generic method in the collections interface that is going to return an array of objects, an array of elements in the collection? Sort of, yeah, sort of is the fact is that there might be a function that is, I don't know, legacy function or legacy method or whatever that is not written in terms of collection. It is written in terms of arrays. I have a method, right, that was written, I don't know, uh, uh, 15 years ago or 30 years ago that just plain arrays. I want to use that method without having to write it with collections so I can do, let's say mixed programming, right, on this legacy code and my and my collection, OK. Does it make sense? OK. OK, so maybe I'm gonna skip this. Because it is not that important. So here in the in the slides I have also copied the signatures of the definition of all the methods such that you can, but this is not that important so I can skip this. OK, so now let's go a little bit into the interfaces, OK? Of the Java collections framework and we have already visited. Iterable, right? And we have visited collection as well and we have visited iterator as well and we have talked a little bit about C, right? OK? So all the boxes here, all the boxes here are interfaces in the collection framework and you have arrows, right? Among the boxes. What do you think the arrows are? What are the arrows representing here? It's a relationship among interfaces, but which relationship? Sorry? Competition. OK, so let's go back to my notes. What do we have here? We have An interface may extend other interfaces, right? So what do you think that the arrows are representing? Interface extension, right? So we can see for example that the collection interface extends the terrible interface right? and that the For example, the list extends the sequence collection and the sequence collections extend the collection and so on, right? And you can see that there can be multiple inter interface inheritance, right? So we can have one interface. Extending more than one interface. Correct? Can you put an example where this is happening here? So anyone can tell me an example of here in the diagram of uh multiple multiple interface inheritance. OK, so the sequence set. Which one, which interfaces is the sequences set extending. Yes, set and sequence collection, right? Could we have that with classes? Yes, we couldn't. We couldn't, we cannot have that with classes, but with interfaces we can, and it turns out that you can see that in the Java collection framework, multiple inheritances, right, is something that is is used and it is helpful, OK? OK. So you see here, right? This is a queue and a queue is actually an astral data type, right? We have here a list and a list is an astrac data type. We have a deck is an astract data type. We have a set which is actually an astra data type, so we can see here that in the, as we have mentioned already, in the Java collection framework, we are actually using, we are actually using interfaces to represent astra data types, OK? So, we also have maps, OK? And maps is something that we are going to be covering in the second part of the lecture, not today. So at least, actually I have already talked about all of this already, so at least represents another collection, elements have an associated index and are added in a particular position. And an element in a list can be accessed in two main ways, right? We can access to an element in a list, given the index of the element. Or we can iterate over the. Over the Over the elements using an iterator and I have one question the. Iterator method, right? Itterador method was a method of the collection interface, right? Wasn't it? And it helped us creating an iterator out of a collection. Do you think that I can create an iterator out of a list? Yes, it's already ordered. I'm Yes, you can, but not because it is ordered. Because there are data structures, as I have mentioned, as data dash for which there is no inherent ordering like in the set. OK, so all right, all the interfaces here, right, except for the ones corresponding to the that are root at the map extend collection, right, and collection extends iterhour so you can extract an it an iterator out of any of the interfaces that we have here that extend iterho, OK. OK. So the list interface offers several methods for random access that are not in the collection e-interface. And these are the methods, OK? So you can add an element given the index, you can remove the element at a given index, you can get the element at a given index, or you can set a given element in a given position, OK? So why do you think these methods are here? In the list and they are not in the collection. OK? They are not here, right? They are in the list. Mm, quite, almost there. Almost there. What are we assuming in the in the uh in the methods of this interface? What are we assuming? What are we somehow assuming of the data type of the actual data type? We are assuming that the elements are indexed. There is an index, right? 01234, that there is an order, right? So we cannot add these methods to the collection because in the collection, there is no such a concept of ordering. The elements might not be ordered. Is that clear? So you can see that as we tend, right? We are somehow specifying more, right? We are specifying more details about the abstract concept that we are extending. Right? So in the list, we are, we are allowed to have these methods. That's weird I mean, it's even earlier, right? Earlier.

SPEAKER 2
Maybe about half of OK, so we have the collection,

SPEAKER 1
let's see if I remember, we have the sequence collection and we have the list, right? And the list has a set of methods where we can get the elements of a particular index, set the elements at a particular index, and so on, right? And this is a specific of a list, right? Zoom recording? It should be a Zoom recording like your Zoom. Yeah, it should be, it should be.

SPEAKER 3
Just check because otherwise you have to stop.

SPEAKER 1
OK, maybe I think I have forgotten. I have forgotten the recording, right?

SPEAKER 3
You have to stop, then then you just give the rest.

SPEAKER 1
Yeah, I have forgotten to record. Yeah, sorry. Stop at 5, no 55.

SPEAKER 3
No, no, we just finished because like the, the echo stopped, so you give this 10 minutes like next lecture you just record more, OK, OK, OK, OK, so I

SPEAKER 1
think I have to finish. No, I have to finish the lecture, yeah, because I forgot, I forgot to activate recording. OK, so let's, let's give it, let's leave it here. But at the end of the day, right, uh, what, let's do a recap, right? What we have covered today is the collection interface. A collection interface. The collection interface represents, right, a collection of objects and we have also covered the iterator interface, right? And the collection interface and the iterator interface are connected through the iterator method of the collection that let, let's lets us to extract an iterator out of a collection, OK? And in a collection, there is not necessarily Uh, that is not necessarily the concept of an ordering of the elements. However, some sub, uh, interfaces of the, uh, collection interface like list or queue. Uh, then there is an order assumed, right? In the case of a queue, which is the order. It He to There you go. First in, first out, right? And in the case of a list, there is a sequence. But there are other uh kinds of collections for which Uh, there is no order implied, but no matter whether there is order implied or there is not order implied, we can always iterate over the elements, OK? Because I heard in the audience, right, something like. I cannot extract an iterator out of a collection that is not ordered, or something like this. It's not true, OK? We can iterate over the elements of any collection. The difference is that for some collections we will be uh walking through the elements in an arbitrary order. We cannot assume anything. If we assume something about the order, we will be wrong. And for other elements, for other collections like lists or queues or decks, we can indeed assume that such an ordering exists, OK? And then we will reflect. And apart from that in the collection uh interface, there are many utility methods right that are written in terms of genetic interfaces that we can leverage with our own implementations of the collections even if these implementations of such collections do not even yet exist, OK? That's the powerful of dynamic run that's the power of dynamic runtime polymorphism. We can write code that can be reused in the future with data structures with implementation details. That are not yet there, OK? OK, thank you, thank you very much for your attention. Course reps, if you're around, just come here.

SPEAKER 3
I can see two of you, but if you are a course rep, please come here, uh, quickly before you leave, and, um, this week we have the lambda. Uh, lab, uh, it's, it's there, there is quite a lot of stuff to do. So anyway, and it's actually long, so be patient. I know it's long, but anyway, it's good for you to practise it's, it's a very hard concept, OK, so take it. It's a good opportunity.

SPEAKER 2
Um, There might be debugging event on Wednesday, but again

SPEAKER 3
I'm confirming the rule. If they confirm tomorrow morning, I will release the debugging sessions and then you attend that on Wednesday, but I will release.

SPEAKER 2
What Yeah well done.

SPEAKER 3
I was like, Yes, that's the right one.

SPEAKER 2
You'll be getting. I I I I That's OK. I. Oh man, 2 hours.

SPEAKER 1
I mean, I told you this is.

SPEAKER 4
You think there it's easy to feel like it's, it's, it's, it's, it's really, really fast. It's really, really fast and it flies, OK, uh, I

SPEAKER 2
think she's there. Do we have any more time here. I I actually I. I'm, I'm not I. Actually. on that. Uh There I.

SPEAKER 5
So.

SPEAKER 2
I should call back. I should call back. Right It doesn't I. It's, it's an interpret. Scan the Yes. Yeah. you. I I'll just, I'll just access the, you know, like the index. mean I. I believe. Guitar essentially. Nixon.

SPEAKER 1
And out of this space what they generate.

SPEAKER 2
Which is a data structure. Right, you you, right? So I'm actually, I am, right? I. Oh yes, you will that
