SPEAKER 0
This material has been reproduced and communicated to you by or on behalf of the Australian National University in accordance with section 113P of the Copyright Act 1968. The material in this communication may be subject to copyright under the act. Any further reproduction or communication of this material by you must be consistent with the provisions of the Act. Do not reproduce this material. Do not remove this notice.

SPEAKER 1
OK. OK, so let us start. So remember that tomorrow there was an announcement on it that tomorrow, Friday, we will have our 2nd debugging session. It's 5:30 p.m. To 7:30 p.m. tomorrow. So I highly encourage you to go and sharpen your debugging skills there, OK? OK So let's continue our coverage of the Java collections framework. And Let me Remind you, right, that so far we have covered uh two different main interfaces in the collection in the collection framework. One is the collection interface which represents a set of objects of a given type. These objects might not be necessarily ordered, OK? We have collections for which there is a natural order. We have collections for which there is no order, OK? And we have the iterator interface, OK? So iterator iterators are objects that we can extract out of a collection. To walk through right? the objects in the collection without exposing the internal layout of the collection that we are walking over, OK? Apart from this, we have several other interfaces. To represent in Java, different tract data types, OK? So in particular, we have We have We have lists, right? It is an astra data type. We have sets. We have queues and decks and we have maps, right? And you can see right that the hierarchy of interfaces for maps which is here by the way I just noticed that some of the of the students one group are gonna make the. The assignment book our our maps. So that's a good opportunity to to talk about that. You can see that the, the map interface is a little bit different from the one of the set of the queue because it doesn't actually extend collection, right? So, and it doesn't, as it doesn't extend collection, it doesn't extend it at all, right? So would you say for example that given a map, right, that we will see what it is. Would I be able to extract an iterator, an iterator of the map or not? So what, what does a collection needs to fulfil in order, in order for us to be able to start an iterator? It has to implement the iterable interface, right? If it doesn't implement the terrible interface, we cannot extract an iterador out of the collection. So you can see that the map hierarchy doesn't extend, right, the iter interface. That means that we won't actually be able to extract an iterador out of a map, but there will be alternative ways that we will see that will help us. Uh, go walking over the entries in a map which are not actually using iterators, OK? Does it make sense? Does it make sense? OK. So a list represents an order collection. And elements have an associated index and are added in a particular position. And elements in a list can be accessed in two main ways. We can use an element index, 01 in the sequence, right? Or we can use an iterator, right, to walk through the elements one by one. The former kind of access, so by element index is known in computing as random access right? because elements can be accessed in any order. So for example you have the memory of the computer, right? How it is called the memory of the computer. How would we call it? 1 gigabyte of Ram, right? Of RAM memory. What is rum? Random access memory, this is what it is. Why it is a random access memory? Because given the memory address of a given data that we want to access, we just access randomly, right? We can access it, it's like an array where we have memory addresses and we can randomly access it, right? So we have exactly the same concept with lists. We can randomly access the entries, right, of a list, OK? And as we know that we can, we can randomly access the element of a list, what we can do is define an extension of the collection, right? The list you can see that is an extension of the collection where we are defining methods, right? Additional methods that only make sense for lists, OK? So you can see these four methods add, remove, get, and set, OK? These are methods that only make sense for lists, OK? Why do you think that they do not make sense for collections? This disorder. Yes. Yes, in a collection we don't know whether the collection is ordered, so the objects of a collection might not be accessible by an index. However, we know that in a list there is a sequence and we can assign an index to each of the sequences to each of the elements so this kind of methods only makes sense for lists. If you are dealing with the collection interface, you cannot access the elements of the collection by the index of the elements. however, if you are dealing with the list interface, yes, you can access the elements using the index, right? of the elements, OK? OK And that is something, this point I have already talked, right? in the previous lecture. And it's the fact that we have to be careful with the list interface. Because not all the classes that implement the list interface efficiently support random access, OK? You have to be aware of that, OK? And that's a sort of Let's say uh limitation of Java, OK? And there is an interface which is this one random access, right? Which you can see as a sort of trait, OK? Of a class. If a class implements the random access interface, we can be sure that random access to that list is going to be efficient, OK? And if it doesn't, it won't be efficient, OK? So that's something that the developers, the designers of Java introduced and a special kind of trade interface random access, and we know that whenever we have a collection that implements the random access this is providing us as a hint that the list that we are dealing with supports random access, OK? OK, you remember that we saw 2 kinds of lists, right? 22 possible ways of implementing a list, which were these? Do you remember from the previous lecture? Don't be shy. Come on, help me, help me. I don't remember. Yes, so we saw the link, the singly linked list, the doubly linked list, and the array list, OK? And do you think that in a singly linked list or in a doubly linked list I'm gonna be able to efficiently implement, for example, this operation. So this operation is gonna give me the element given the index. What do I have to do to find the element in a list, in a linked list, given the index? For example, I want to find element 1000. What do I have to do in a link list? Remember Remember? There it is, the the linked list, right? What do I have to do in a linked list? Imagine that I have 1000 elements. What do I have to do? Yes, so you have to start with the first element and then you have to, in a loop, you have to follow the next pointer. Right, 1000 times until you find the element that you want. So do you think that this is an efficient way of finding an element in a list? It's very slow, especially if you have a large number of elements, because you are gonna beerating ba ba bah bah bah until you find the element, right? So, when you, when using the list interface and when using lists, we have to be aware that there are some methods of the list interface, right? There are some methods of the list interface. That We can use Because they they are there and they are implemented, but we have to be very careful because some of these operations might be very, very inefficient, OK? OK. OK, so. A caution, caution when we use, so remember that. We have genetic interfaces, right? So list is a generic interface because it's uh parameterized by the type of the elements of the list. When we specialise the genetic interface with integer. Getting a list integer, there are two methods in the interface one which is inherited from collection and another one which is part of the list which are very close to each other, OK? So you can see that we have the remove method, right? And the remove method has two different variants. We can remove. The Element by by index. And we can remove the element given the element, OK? And you see that in the signature of of these two signatures of remove the only difference is that we are using a primitive type in in the case of the index and we are using a boxing type in the case that we want to remove by the value of the element, right? So whenever we pass an integer, which of the two methods are we gonna end up culling. That's a question that we have to, to understand, right? And the thing is that if we pass a primitive integer, for example, the literal 3, right? If we pass a primitive integer. If we bus a primitive integer, so the literal 3, let's say, right, then we are gonna be calling the first of the two, not the second. OK, so we are not actually gonna remove the element 3, we are gonna be removing the element in position number 3, and if we want to remove the element 3 we need ourselves to auto box. To out of box into an integer variable, right? So we have to do something like this, we have to do integer. Integer Element Equals 3 and then we have to pass that 1. To this second variant, OK? Because otherwise we're not actually gonna be removing the element. By the value of the element but by the index of the element, OK? So you have to be very aware that this is something that happens only whenever we instantiate the least generic interface with the integer type, OK? And this is actually something which is a sort of a lot of confusion. This is why it is there and I want to clarify it, OK? Does it make sense? Yes, that's a good question. It could be possible, but it depends on how the ADD is defined. How the abstract data type is defined. In Java, as far as I remember, I think that only the first occurrence of the element is removed, OK? OK? So if you have duplicates, only the first occurrence of the element is removed, OK? But you can check it and if I'm wrong, I'm sorry. I'm not 100% sure, but I think so, that's the case. You can check it. OK. OK, so the list interface, something that you should be also aware is that it fixes the semantics of the equals and the Hakco methods, OK? OK, what is the equals method and what is the Hasco method? Do you remember? I mean, we have already covered that, right? And it was in the exam last week. What is equals? To which object So these are instance methods, right? To which object in which objects are they are they defined in which class? Yes, the cosmic superclass, the cosmic superclass, right? So the, the list interface right? as part of the definition of the list in Java we also specify how implementers of the interface should implement equals and hash code OK? in particular. For a list to be equal to another object, whatever it is, the other object has to be a list of some kind, OK? For example, if we compare a list with a set or if we compare a list with a map, if we compare a list with a queue, even if they have the same elements and in the same orders, they cannot be equal because the other object has to be a list, OK? And that that is fixed as part of the definition. If you implement a list. Right? Your own list, my, my Dublin list, which by the way, we are going to see a little bit. And then you don't define the equals method according to this definition then you would be violating the lists of substitution principle, OK? Because you may have codes written in terms of the interface for which your particular class implementation is not gonna behave consistently, OK? So this kind of stuff is something that you really need to to know when you are implementing interfaces, right? There are a set of rules and all the implementations of that interface have to uh have to fulfil that rules. Because if we don't fulfil that rules and we have code written in terms of the interface, the code may just behave in an inconsistent way, OK? OK, both lists must have the same size, OK? If you have two lists of different size, they cannot be equal, right? And the corres the elements in the corresponding position must be equal as well, OK? So this is how the equals are defined, OK? So for example, if we have the list. The list. -1, 2, -1, OK? So this is a list with 3 elements, right? So this list is is not equal, for example, to -1, -12, right? Both are lists, OK. Both have the same elements, but the elements in the corresponding positions are not equivalent, OK? So that these two lists wouldn't be equivalent, wouldn't be equal, right? The other matters in a list. OK? Again and again the same concept. The other matters, OK? OK. OK, so we have the set. What do you think asset is? Order doesn't matter, but in, in, in elementary school, right? We have the concept, the mathematical concept of a set, right? A set of objects, right? It's exactly that that concept, OK? And sets actually are come very handy when writing computer programmes. For example, imagine that you want To You are given a string with some characters and you want to determine the set of the, the set of characters, right? Of the string. Eliminating duplicates, something that you could do is, OK, I go over the string and then I insert. The characters in my string into the set and when I complete the loop, right, in the set, I will only have the the characters without duplicates, right? So with sets I mean you can actually do many, many different things, OK? And they they come in very handy, OK? Again, the set interface fixes the semantics of the equals and has said has code methods. If I implement my own set, I need to fulfil that semantics. Otherwise I wouldn't be fulfilling the Lisco substitution principle, right? And apart from the map, right? We have the sort map and the navigable sorry apart from the the the set we have the sorted set and we have the navigable set. The sorted set exposes the comparator object used to sort the elements of the set. So a sort set is a set in which the elements are ordered, OK? Because in the in the traditional set the the order of the elements do not matter and we're gonna have duplicates, but there is a specific kind of set where the order matters. And then the interface of the sort set exposes the comparator object. OK, what is the comparator object and what is the what is the comparable interface and what is the comparator the comparator interface? Comparable and comparable. That's something that we have already covered as well. Do you remember that one? Uh, uh, we compared to inside the right Yes. So we have The comparable interface, right? Which I extended by a class right and then we can compare using this interface via a method which is called compare to objects of my class with objects of another classes, right? And remember that this method re done either a negative, a positive or zero depending whether it's smaller, larger, or equal, right. But in some When we implement the comparable interface for a class, we are fixing the behaviour of of such an ordering, but there are situations where we want to use a different ordering from the ones that we define in the comparable interface and to this end, what we can do is we implement a different class and then we extend or we implement, sorry, not extend implement the comparator interface so a comparator object. A class that that implements the comparator interface, right? The objects of such a class are gonna help us comparing objects of other classes, OK? And it turns out that these interfaces, the comparator and comparable interfaces, will be used by all the extra data types in the Java collection framework for which there is an implicit order in the fine, OK? So in the short set we are ordering the elements by some criterion, right? For example, if we have strings, we may want to order them alphabetically in the standing order. But we, we may want to, uh, order them, uh, alphabetically in a standing order or we may want to order them by their size. So first, the ones that have 3 characters, then the strings that have 5 characters, no, 67, so there are different criterions that we can apply when we want to sort a collection of objects. And the comparator and the comparable interfaces are the ones that we can define in order to specify how do we want the objects in my collection to be ordered, OK? And you can see that what we are doing here is composing things, right? We are composing astra data types, genetic interfaces with interfaces. That we have seen in the previous lectures. So we are building on top of, right? It's getting interesting. OK, and the navigable set contains additional methods for finding next or previous elements in short sets, OK? OK. There is also this sequence collection that was introduced in Java 21, OK, and it's here, right, that many different interfaces, uh, implement extends, sorry, like the sequences, the list and the deck, and essentially they provide methods to, uh, find the first and the last elements, OK. And also to do reverse traversals, OK? OK? So typically, When we do a traversal with an iterator of a list, we are gonna be traversing in which order? By increasing Index, index 0123, OK? So there is one method in the sequence collection which is called reversed, do you see? That the that returns an object, a class that an object of a class that implements the sequence collection interface and then if we start an iterator out of that sequence collection what we will be doing is traversing. The list from the highest index to the lowest index instead of the default traversal order, which is from lower to higher index, OK? OK? And the sequence set sharpens the return type of the reversed method to sequence it, OK? Remember that? When you have an interface and you have a method which returns. Uh, object of a type interface and you extend that interface when you overwrite the method in the sub interface you can sharpen the type. Right? Of the interface that you are returning, OK? That's something that we have seen that you can also do. And the sequence set that so because when we reverse a sequence set, you may imagine that we are gonna be obtaining a sequence set as well, OK? OK. And I want to say something now. You remember that There were 2 class implementations. To 3 possible class implementations of the Atra data type list, right? That we introduced the single link list, the double lily list, and the red list, correct? Yeah, I'm, I'm running out of battery. Um Wait a moment. OK, so How would you implement the reverse traversal in the case of a single link list? That's a question for you. Do you think it is going to be easy? Do we implement it? Why not? So let's say that the following thing how would you implement it with the WE list? How would you implement the reverse traversal with the doubly list? for the previous partner. Yes, so you would go to the first reference and then from the first reference you will go to the previous one, and then we are on the end. And then we follow the previous pointer, the previous reference until we reach the first. And how would you do a forward traversal? Introdu You start from the first, next, next, next, next till you reach. The first, right? You can implement both the forward and the backward traversal with a doubly list, but how do you do you do that with a singlely list? Well, what you can do is maybe is I don't know transform the list in an array right? And then in an array you can you can you have random access support and you can start from the last to the first, right? But You see That depending on the information that you have, There might be some operations that are very hard to implement. Again This notion of trade-off, right? Among The amount of memory, right? That I'm going to require to represent an instance of an astra data type and the cost of the operations, OK? There's always a trade-off among these two. OK. So let's move now to class implementations because Interfaces are great, right? But without class implementation, we cannot do anything, right? Imagine that I provide you an interface. What can you do with the interface? Can you solve any problem with an interface? You need to implement the interface in order to be able to actually exploit the power, right? Of the interface. And the Java collections framework provides a hierarchy of class implementations, OK? This is actually the hierarchy for lists, sets and queues. There is also another hierarchy for maps that we will see later on, assuming that we have time. If we don't have time, I will record at my office the remaining part of the lecture and I will upload it, OK? Because I'm going much slower than I was hoping. OK. So we have here The link list And the array list, OK? They both implement the list interface. And we have here different kinds of sets. The has set, right? The noon set, the 3 set, and we have also different kind of queues. We have the priority queue and we have the right deck, OK? OK. But apart from the leaps, right, in the, in the hierarchy we have the leaps, the leaps in the three hierarchy, but we have intermediate classes, right? We have, for example, abstract list, abstract set, abstract queue, abstract collection, right? Why do you think Are these abstract classes there. Because I could just remove the inner nodes in the tree and the root. I just provide the leaves. And that's it. This is all that you need, right? Why do we, what do, why do you think we have abstract classes in the middle of the hierarchy? moments No. Not quite. Thank you And you Better case of abstract extended. Jason, you Yeah, the main reason, what is an astract class? Because maybe that's something that I didn't mention here. The Boxes right represent classes, right? And the name inside the box is the name of the class, right? Whenever they have the abstract prefix such classes are abstract. I mean it's pretty obvious, but I didn't mention, right? So for example we have abstract set, right? And what is an abstract class. What is an abstract class? That's that. Yes, but it's a partially defined class, right? It's a partially defined class. We might have code and data in that class, but there are some components of that class that are missing. Why are missing? Because at the level of the abstraction, we cannot specify them. We need someone to come and say, OK, I want to specify that abstraction. And this is what the hash set, the noon set, and the 3 set are doing. So in the astral set there is some common functionality, right? There is code and data. That the the extensions, the extensions of the abstract class can use. In order to implement some of the methods of the set interface so you don't have to start from scratch you can set one of these abstract classes you will get for free a lot of functionality and then you complete those methods of the abstract class that cannot be completed because at the level of the abstract class. We don't know yet how to, how to complete some of the, the components of the AA class that are incomplete or partially defined, OK, because you may imagine that a hash set and a tree set, right? I'm gonna talk now about the concept of hash tables, which is what the hash set is based on. The tree set is is based on a tree data structure to keep the elements of the set order. You may imagine that both data structures, the one that uses a hash table and the one that uses a tree, a red black tree, they are very different from each other. But still there are some common functionality. So we factor this common functionality into an astralt class and then the implementers of the astralt class can reuse a lot of functionality, right? So, I have to say something is Object orientation is about being lazy. OK? In the sense that you have to think. How you can grow your stuff, doing the minimum possible effort. And the way to do so is by properly using Inheritance, dynamic polymorphism, etc. etc. So whenever you are solving your problem, You have to be lazy. OK. OK, that's something that I have been I have already talked about what is the main drawback of a realist. So in an array list, I have my elements stored in an array. What happens if I remove an element in the middle? Have to shift them. To the left, right, or to the right, depending on, right? And we don't have that with lists, right? We don't have that with linked lists, OK? OK, so that's something that we have already talked about a little bit, right? So we have a data structure which is the linked list. And it turns out that in the Java collection framework, When you declare a class of type linked list, internally. The Java collections framework is going to use a doubly little list, OK? So it's not a single link list. It's a doubly linked list, OK? So they might So here, right, I have a linked list, right? And that's something that you can declare. Anything that is here, you can declare, right? You can use in your programme. So you need to know that internally, the Java collection framework is going to represent a linked list using the Dublin linked list approach, OK? OK. So now let's think. About, a little bit about How would you remove an element in the doli list? I mean, in general you don't need to do that because you have a link list class in the Java collections framework. You're gonna declare an instance of the linked list class you will be adding and removing elements and you have, you don't have to care about this. But let's go a little bit deeper and let's try to think a little bit what would we do in order to implement, for example, the remove operation. Imagine that I want to remove one element in the list, OK? And I have a reference to the element that I want to remove, OK? Which is the green arrow, OK? Let's call this reference element. Do you have a better name? Element, OK? Element or element, let's say, OK? And we want to remove this element from the list. What do you think we have to do? And let's go step by step. So the one that that was 7. Yes, we need to update the next pointer to. And the 3, the 322. There you go. There you go. OK, so what Pranav is saying. Which is correct. Thank you, Bernard. Have the feeling that someone is getting what I'm saying, it is always good. What Trump is saying is that we have to update the next pointer of the previews such that it is gonna point right now. To the next of the elements. OK? So how would you write that in code? How would you write that in code? You would do LMM, right? Dot P. Right? Dot Next Equal to what? Uh So, where do we want to point? Where do we want to update this pointer? So, this thing, right? This thing LM.p. The next is LM.P. Next, is this, this guy here, right? Equal to element. Dot word dot, right? Do you see? So we are accessing Through the green arrow to the previous note. With access to the next. I should have written here next, sorry, not end. We access to the next pointer of the previous note and then we redefine the reference. In such a way that now it's not going to longer point to LM, it's going to be pointing to element.net. And then what we have done, right, is We have removed This link, right? No longer exists and we have redefined, right? You have circumvent somehow work around. They know that we want to, OK, so is that all? OK. So the second thing that we need to do is what? Yes. So the thing that we have to do uh graphically, right, is, OK, this is no longer true because the previous is not going to be the, the previous is going to be which one? -2. And how do we write that? This Panabua saying. EM Do what? Next Dot. Previous or previous or P equal to what? Element of previews, right? Or P. It's the same thing, right? Previews. OK. Is that understood? Does it make sense? OK, so, good. Good And then again after this process how many references are pointing to the node that we have that we are willing to eliminate? How many arrows arrows are pointing to the node 7? Are there any arrows pointing to the note 7? OK, and if there are no references pointing to an object, what is gonna happen with this object? The garbage collector at some point will get rid of it. OK. Does it Feel like a costly operation. To do that Is it a costly operation? Doesn't feel so, no. We are just taking two references and updating them, that's it. So you can see that in a linked list. Eliminating an element in the middle is very, very, very efficient, OK? OK. So as I said, right, the, the lean list class. The. Mute this because otherwise it's gonna drive me crazy. Um, my boss, my boss is pushing me. OK, so the link list handles these references under the hood when removing or adding elements in the link list, saving programming for this burden. OK? So here you can see that we are declaring. A cla an object of the class list, right? With element type string. Right? And Then what we do is we add 3 elements. 3 elements to the list, so Amy, Bob, Carl. In this order, so add is defined in such a way that the element is added to the end of the list, right? So in the first at we have only Amy, then we have Amy, Bob, and then we have Amy, Bob and Carl, because the other methods are the things at the at the thing the element at the end, OK? Then we start an iterator. Why can't we start an iterator out of a list? Why, why can't we do that? OK, yes. Because at least First, because a lean list. Is a class that implements the list interface. That's the first thing. And the list interface. Extends the collection interface. And the collection interface extends the er interface and the terrible interface provides the iterator method and the erador method is gonna be the the the method that is gonna return a class. An object of a class that implements the erador interface. This is why we can iterate. At least And by the way you see myself all the time asking questions or trying to ask the questions this is the sort of questions that you should you should ask yourself when you read a programme and when you when you write programmes. You should be all the time prompting yourselves. Why this? Why that? Why this? Why that? Why this? And this is the way that you learn, you, you, you build the knowledge. If you do not ask about everything, it's not going to work out. OK. OK, so we start the iterator. When we start the iterator, what is the iterator pointing to? Yeah, don't be shy. To the first element There is a subtlety. Before the first element. Before the first element. OK? If we were talking about C++, yes, I give it to you, right? But that's not the case in Java because in the in the Java, we have to think about iterators, about pointing between elements, right? Not to the elements, between elements. OK, so at the beginning, our iterator is gonna be pointing before the first element. And then we call the next method. And the next method is gonna return us the, the first element we are gonna walk over the first element and we are gonna get the first element then we go next again what happens? Yes, in between the 1st and the 2nd, right? And then finally, we remove, we call the remove method. And the remove method Is gonna remove the last element that we have walked over. OK? So which is the last element that we have walked over? Amy, Bob, or Carl? What do you think? Let's try to do it So we have Amy. We have both. And we have CAL, right? We start right before, right before Amy. We move the iterator here, right? This is the first call to next. And in the next call to next. Right? We position the erator here between 1 and 2 So the remove method is gonna remove the last element that we have walked over, which is the last element that we have walked over. Amy, Bob, or Carl? Bob, OK, we are on the same page. Good We can also add elements, right? Apart from removing elements, we can also add elements and the collections interface, sorry, the collection interface, not collections, collections is a class, not an interface, we will see later. Provides a method, right? Provides a method which is called A, right? OK? And in the case of lists, this other method, and we have already talked about this, is defined as adding the element to the end of the list, correct? But do you think that That's the only thing that we may need to do to add elements to the end. Imagine that you want to keep the the list sorted. For whatever reason. Right? And you need to add an element. Right? Imagine I have AB Bob Carl, maybe that one is not the best, the best, uh, but I have, I don't know, 0, 10, and 20. Imagine. And then I want to introduce the element three, but I want to keep. The least order in increasing order in a standing in the standing in a standing order. I have to introduce the new element in the middle, right? So to this end. Lists provide a specialised iterator. Which is called the list iterator, OK? So the list iterator is an implementation of the iterator interface, but it adds or provides additional methods which are specific to lists, OK? So you see here We have here The list iterator, right? And here we have the list, right? So the list iterator is actually uh an extension, an interface which is defined an extension as an extension of the iterator. Interface, OK? And it only makes sense for collections that have And ordering. And natural ordering like lists, OK? For example, the Tor 4 sets, we have a set that doesn't have a natural ordering because We can access the elements in a random access, random order, then we cannot add elements to the set using the iterator, OK? So how is the A method of uh What, how is the ad method? Define It is there, right? So we have an interface which is called list iterator that extends iterator, right? And then we add one method apart from others. Which is called add element. OK? And There is a difference among this a method and the a method in the collection interface. So if we go to the collection interface. If we go to the collection interface. What does the the a method? Bullion, right? Why, why does it and bullion. Yes Yes Yes, more or less. More or less. Is because there are cases in which we try to add an element to a collection. And we might not end modifying the collection. And one prototypical example is, for example, a set. You have a set, you introduce an element which is already in the set, then you are not gonna modify the set because in the set you cannot have duplicates, right? However, you can see that in the a method of the list iterator interface, the signature is different, right? What is the difference? It returns void or it does not return anything, right? It returns anything, right? Void. OK. Why do you think is that? Because element will be added, there is no reason for it to be false. Yes. Yes. You were allowed to say the same, no, yes, yes. So And how is the method defined? Because We are iterating, right? We are moving our iterator through the sequence. How is the A method defined? It's in a bold phase we always introduce the elements before the current iterator position, OK? And if we introduce Several, right? Elements in a row at at at at, then we are gonna be putting them in the list in the order in which we are adding them. OK. Apart from that, we have a couple of methods which are called previews and has previews. Why do you think these are needed? Because the iterator method had next and has next. But it didn't have previous and has pres. That's something very specific to a list. Why can't we, so what do you think we, we can do with that? A at the specific index. Element at a particular place. Not quite, yes. Yes. So we next forward traversal. With previous backward traversal. OK. And again We have to be careful because when we are reaching the, the first element, if we go Previews Right? We might not get any element, right? It may crash, right? The same thing. So the previous method returns the object that iterator is skipped over again the same conceptual map we have the iterators pointing in between elements when we go previous we walk over backwards, backwards over the previous element, not the next, right? And then if we go remove the iterator eliminates the element to the right of the iterator position. Right, so when we call remove with the forward iterator we're gonna be eliminating the element that is on the left once we have walked over. However, if we call previews. We are gonna be also eliminating the element that we have walked over, but this element is not the one on the left, it's the one on the right. OK. I mean you might be wondering why we need to, why we need to learn all this, right? Um The answer is that it might not be that uh relevant but it's gonna help us right? to develop. Uh, uh, an understanding of how we have to read, right? How we have to read code that others have written and code that others are providing to us, right? This is why we are exploring this in such amount of detail, right? OK, so we have a method in the link list class which is called list iterator, not iterator, list iterator. Which is gonna return an object of a class that implements the list iterator interface, OK? OK, and here there are some examples, OK? So we have here a set of steps, OK? 12345678. Imagine that these are executed in sequence, OK? And I have a list that initially has 6 elements. A, uh, it can be strings, right? Or characters A, B, C, D, F, right? And then, I'm denoting with uh with the pipe character, the pipe, the tube. The current position of the iterator, OK? So, in the first uh so can anyone describe what we are doing in the first stage, in the first step? So L is a list, right? What are we doing in the first step? We are calling The least iterator method. And this is going to return an object. That is going to be pointing Before the first element, OK? What do we do in the 2nd, in the 2nd step? Remove, ah sorry, I didn't understand properly, sorry, my my fault. We move, right? I'm obsessed with finding mistakes. You, you move it forward, right? One position. And which element are you gonna returning out of next? 8 eliminated index. A, right? Element A. You are not returning indices, you are returning elements, right? Of course, you are returning the element in position 0, but what you are returning is the element, not the index, OK? OK, so now we go. A In step number 3, and where it is added, the LG, where it is added. In between A and B, that's correct, but Before the current position of the iterator. Then we go age again. What is age introduced? Thank you. After G, but Before the iterator, right? So the iterator. The terator, right? Laterator is point is pointing here, right? We introduce the element before laterator, right? OK, so next, what do we do next? We move the iterator. Forward. And then we remove Which element are we removing? Letter, letter. Which letter? B. And what is B? Now, what is B? Yes, the element that we have just walked over. OK. And then we got previous. And what happens with previous? Both We moved the territor backward. And then we got removed Which element did we remove? We removed 8 and what is age? Yeah, the elements, the last element that we walk over. But this time it's not the, it's not the one that on the left, it's the one on the right. This is because we have walked over backwards, OK? I mean And there is a, actually let's go over the box. So now, not how the add method just depends on the current position of the listed terator, right? The a method only depends on the current position. Of the iterator, however, the remove method depends not only on the current position of the of the iterator but also on the state of the iterator. And by a state of iterator, we mean whether we, we are, we have gone backwards or forwards, right? And again, something that I want to make clear is you may be wondering why these operations are defined this way. Right? Because I may define the operations differently. Right? I mean, it's a convention. It's a convention that people after decades, right? of software development. have realised, right? That it's a good combination or good convention for writing computer codes, but at the end of the day this is a convention, right? I may define the methods with different behaviours. It's not set in stone. Right? But the people, right? Because the people that have designed, they know what they are doing, they are smart people. Before, right after writing codes during. Decades Even before object orientation was developed, right? They realised, right, that these sort of operations are very, are very, very frequent, very frequent when we write computer programmes. This is why they are there, OK? OK OK, so that's something that I have already covered, but let's let's take a look at this code for a moment, right? And let's imagine That the list that we are walking over. It's a linked list What do you think is the problem with this code? I mean the the code is is gonna work the code is functional it works. But what is the problem with this code? And don't read the slide. So what, what are we doing in this code? We are iterating over the indices right from 0 till the last index which is the size and then what do we do in the inner loop in the in the body of the loop what are we doing? Where, what are we doing? It's, it's written there, right? We are calling, which method are we going there? Get, right? Of course, next is also done, right? Well, in this case, no, in this case, no, because we are not using the general four syntax, right? But we do get I, right? Imagine that the list that we are working with is a linked list. What is going to happen in GT? In You Imagine that we are in iteration 1000. So I is equals 1000. We are gonna go get with 1000, but in the link list what do we have to do to find element 1000? I. Yeah, we start from the first. And then we have to jump follow forward, follow the the next right till we find 1000 so do you see that this code is going to be problematic? It's gonna be brutally slow. Particular, particularly is gonna have what is called quadratic complexity. We didn't talk yet about complexity of algorithms. I'm not sure if we are going to have time to because we are reaching the end. But the idea is that the amount of operations that they have to do here is going to grow quadratically. What does it mean that something grows quite a degree? N to the power of 2. So if we multiply by 2. What's going to happen with the amount of operations? Not double. Power of 2. So if we multiply by a factor of 2, the number of operations is going to be multiplied by a factor of 4. Because this is what quadratic growth of something is, right? cubic, it would be 28, right? And quartic it would be 216. So in this kind of loop. If we multiply the size of the list by a factor of 2, the amount of operations that we are gonna be doing is going to be multiplied by a factor of 4, right? And if you see, if you compare, right, the cost. If you, if you go to a, you draw, right? In a graph, right? And you draw the linear growth, right? And you grow the quadratic growth, you are gonna be something like this, right? So, linear quadratic, so you can see that this code is, is whenever the size of the list becomes large enough, it's gonna be unbeatable. And you're gonna wait forever, I mean. OK. And this is something that actually the Java, the developers of Java could do better, right, could have done better, right? Because what we, they could have done is to separate into two different interfaces, right? And then in the interface for linked lists, they do not provide random access, but this is what it is and we have to live with that, OK? So the bottom line is if you are not following what I am saying, the bottom line is. If you have a linked list. Do not use random access. If I see someone using random access, I will find you. However, If we do the the same operation with an array list. OK? It's gonna be very fast because arrays are able to provide. The elements Uh, via random access in a very efficient, in a very fast way. Essentially it's going to take one operation. And remember that I talked about the concept of random access memory, right? The random access mem the random access memory is just conceptually is just an array. That we can index using the memory address. The memory address is gonna be the the array position and an array is stored in the RAM. The array The Java ray is stored in the rum. So when we write. Uh, um, square brackets 3, then the compiler is gonna is gonna translate that into access into a memory address which is not gonna be 3. But the access to that piece of of memory is gonna be very fast because the random access memory is an array as well. Right? This is the reason why accessing to the elements of an array is so fast because the, the device that we have where we store stuff in the computer is an array as well. It's a random access memory, RAM, right? RAM, OK. OK, so take away exercise, uh. I have written um partial implementation of the list interface, right? In the it's available on campus so you can download the code and you will see there that I'm partially implementing the list interface, OK? And I'm implementing it with a class which is called doubly linked lists. You may imagine what doubly link list is, no, I mean it's not very hard, right. And the task for you after the lecture, we are not going to evaluate this, right? It's something that you can do. I highly encourage you to do it, but if you don't want, it's fine. Implement these 8 methods. Of the list interface, OK? And in order to help you started, I'm gonna provide you the definition of the classes. And implementation of Three different methods, 23, and 8. And then looking at this code. You have to be able to implement the rest, OK? OK. Do you think it's gonna be helpful that I uh talk a little bit about that code? And then we discuss it or would it be fine if we continue with the lecture? What do you prefer? Can you raise your hand if you want to look at the goat? No one? OK, so let's continue. OK. OK, so queues and decks, OK? So we have already covered lists and then we move to the next ATA data types. Queues and decks, OK? What is a queue? Remember that we talked about it when we introduced it, right? In the last lecture. What is a queue? First in, first start out. It's a collection, but it's an order collection because we keep an order of the elements and this element is this order is first in, first out. And then how do we add elements to the queue? At the end, and where do we extract the elements in the gear? Where, from where do we extract? From the beginning, right? Imagine all the real queue, right? If you don't cheat, right? If you don't cheat, you're gonna, the one, the first one is gonna be the one that is gonna be is gonna be served first and the last one is the one that is gonna be served last, right? You have to wait for the others if you are a good citizen. In Spain we wouldn't do this, we would cheat, but that's another story, OK, uh, but there is also the concept of deck, OK? And the deck is Uh, specialised version of the queue where we can insert elements and remove elements at the tail end. And we can also insert and remove elements from the head, OK? Is that understood? OK, so do you think that we can introduce an element in the middle of a queue? Can we introduce an element in the middle of a deck? Mm No Can we remove an element at the tail of a deck? Yes. Can we introduce an element at the head of a deck? Yes. Can we remove an element at the head of a queue? We cannot remove an element from the head of the queue. You can hear. We can remove elements from the head of the queue, but we cannot add, right? Is that understood? OK. Hopefully, yes. OK, so let's do some OK, and then priority use, OK? So priority use Are a very kind, a very special kind of cue, right? Where Element retrieval, so to to extract the elements. It's done in short order, OK? So we have a data structure where we start adding elements. And we can add elements in an arbitrary order, but when we remove. One element, the queue is always gonna provide to us the smallest, the smallest element out of the collection. I mean one way to fulfil that would be to keep the collection ordered and then you just provide the first element each time but this is not how the priority key works. The priority key works using a very important data structure in computing which is called the heap, a heap, OK? This is not a data starter course, so you are good news for you, but You can think as a heap as a very special kind of tree data structure where whenever we remove an element, so whenever in the root of the tree we are gonna always have the smallest element in the collection and when we get rid of the root. The elements in the tree are gonna gravitate towards the root in such a way that we will keep having the smallest element in the root of the tree. This is called a heap, OK? So that's the priority que keep the elements sorted side? No, it doesn't. However, if we remove the elements one by one. Are we gonna get the elements sorted? Yes, because the priority queue is always gonna give us the smallest element out of all the elements in the collection, OK? A typical use case of priority use is job scheduling, OK? For example, in supercomputers. So I use supercomputers every day for my research, OK? I do scientific computing and then in a supercomputer you have, I don't know, thousands of users, but you have limited resources. You have, I don't know, 1000 compute nodes. What you do is batch computing. You send a job to the queue. And then you have to wait there in the queue till the resources that you are requesting are free and then you can get inside. But whenever the resources get free, there might be more than one task or more than one job that can be executed. So what I can do is I can keep a priority queue, right? What I can give a priority, for example, if I'm the boss, I'm gonna give the highest priority, right? And then when the when the When the when I extract, when I remove the element, right? I'm gonna extract the one with the highest priority to be scheduled to run, right? And also priority queues are also very useful when efficiently implementing graph search algorithms. We're not gonna go down that path, but there is a very famous algorithm, uh, which is called the DSTtra algorithm, which is the one used in Google, in Google Maps to search the. The optimal paths, which actually uses priority cues as well. Do as part of the algorithm of finding the optimal paths from a source to a destination in a map with weights, OK? OK, so this is a piece of code, OK? Uh and something that I didn't mention, sorry. Is the fact that in order to be able to insert an element in a priority queue. We need The object that we are inserted to implement. The comparable interface. And that's something that I have mentioned before, OK? So if the object that we are trying to insert. Into the queue, into the priority queue, does not implement the comparable object, the comparable interface, then we cannot inject it. And why do you think is that? Yes, yes, because it needs a criterion to decide which is the smallest element. It doesn't need to be ordered because the priority queue does not order things. What it does is it provides you the smallest element, right? But the smallest is something relative to the criterion that you are, you have decided to order your elements with. I can, for example, order my numbers by magnitude. Or I can order my strings by alphabetical order, but there are other orders, right? I can order them in increasing order by the length. First, the shorter ones, then the medium ones, and then the larger, so with the comparable object. With the comparable interface and with the comparator interface comparable and comparator we can define the ordering criteria and we specify that to the data structure and the data structure is gonna be behaving according to the ordering criteria that we have defined in our comparator and comparable interfaces, OK? OK, so what's going on there in this code? What are we doing there? By the way, these 4 guys, Hopper, Lovelace, John von Neumann, and Konrad Zus were 4 computer scientists, physicists, and mathematicians that mathematicians that have, that made the breakthrough inventions in computers, OK, for you, general knowledge. But what are we doing there? And John von Neumann developed what is called functional analysis, which is one of the of the fields that I most like of mathematics, but that's another story. OK, so what are we doing there? OK, so first, we are declaring a priority queue. We are creating a priority queue, right? And of which type are the elements? OK. Do we have worked with local data already, right? Bernardo has has introduced that to you already. OK. So the local date implements. Is a class that implements the comparable interface we can compare. Two dates. Is this earlier date or is this forward in the in the future? We can do that. So fine, we can instantiate the priority queue with our local date and remember that we are using generics, so the angle brackets, right? The angle brackets when we declare the variable, right? Then what, what are we doing? We are adding. Dates Of the scientists, of the researchers, OK? And then what, what are we doing here? What are we doing in green? What are we doing in green? Sorry, not in green, in grey. What are we doing here? In the whole box. Iterating over the cube and then printing each element. Yes. We are using The forage, right? Loop and the for each loop internally is gonna be calling. What is gonna be doing to to walk through the elements in the collection. The for each loop. for each loop. It's going to traverse the elements, right? But how is it going to do it? How would you transform the four loop into a wide loop? No it was the last. OK. Let, let me put the, yeah, thank you for your contribution, but let me put Let me put this picture here again. OK. Do you think, let me put the question in another way. Do you think that the for each loop When walking through the elements of the priority queue collection. Is going to use any of the interfaces that are in the in the picture. They did our interface? Yes, but who else? Good. How do you iterate over a collection? No, how do you iterate over a collection? Next, but next is a method of which interface? Iterator, right? Iterator, right? So what the for each for each loop is gonna do. Under the hood, you don't realise that, but it does. Is it's gonna strate an iterator. Right? It's gonna do enter in a wide loop. That is gonna be iterating while has next and a dig iteration is gonna call next to walk over the next element. You don't see that because this is syntactic sugar, OK? It's easier to write that a for loop rather than writing a wide loop, but the compiler is gonna translate that for loop into a wide loop that is gonna be using the eror. OK? OK. The question for me, for you is, not for me, for you, I already know the answer because I have prepared the lectures. Um Do you think that we are gonna be getting the elements of the priority queue? In an order or not? We are traversing, we are not extracting the elements one by one, we are traversing the elements. No, the answer is no. We may get The elements in an arbitrary order. So in the grey loop we may get the the dates not necessarily ordered chronologically because the priority queue does not internally order the elements. It uses a very clever or smart data structure that is gonna be able to provide the smallest element in a very fast way, but it doesn't order the elements internally however what's gonna happen in the orange box? What are we doing in the orange box? Yes So what we are doing is, while the priority queue is not empty. We remove Remove the remove one element, then we remove that element. Why this loop is gonna finish? What do you think can we enter in an infinite loop there or not? By the way, have you ever, have you ever written a loop that that becomes infinite? In the labs That's very typical. I do it all, all the time, right? So what happens that why this loop is gonna finish? Yes, because the queue is finite. The cube has a finite number of elements. So at some point, no matter how large it is. Because the computer does not have infinite memory, right? It's a discrete, it's a discrete device. If it had infinite memory, then this loop could loop forever, but it's not the case, unfortunately, right? So what's gonna happen? We are gonna be removing the the smallest element because this is what the priority give warranties to us and then we remove the smallest element again and we remove the smallest element out of the remaining ones so in are we gonna get the elements in an order. Yes We are going to get them ordered OK, does it make sense? OK. OK. So, exercises. Which of the following statements I would after data that is correct. See Are you sure? OK. Let's, let's stream a little bit, right? An ADD is defined in terms of its its structure. No, it's defined in terms of its behaviour, right? A link list is an example of an ADT. Mm. There's a subtlety A little list is an implementation of an idea. OK, a list is the ADT. A linked list is an implementation of the ADT, right? There are two concepts. We are separating abstractions from implementations, abstractions from implementation details, and this is the way that we are gonna write software that is gonna scale with size. If we don't do this, we are not in the right direction, right? What happens with the set? The set is an example of an IDD. Yeah OK. Which of the following statements? Odd this in Java are correct. Which one? See Or A See See? OK. Java uses genetic interfaces to express the concept of an ADT. Why is not a. Because it is Why this, why this not be? I Come on. What, what I have been talking during the last half an hour, right? And the It provides parameterized abstract classes like abstract collection, abstract link list. They help us implementing. OK. Yeah, I like to write. I like to write a lot. So which of the following statements about the derator interface of in Java is correct? There is only one. OK, one can add elements to the collection being traversed with the other method. Is that correct or not? There is a subtlety. We are talking about iterator, not list iterator. OK? So in the erator, we cannot add. In listed iterator, we cannot. Calling the next method with the derator located at the end of the collection returns the last element in the collection. No. What happens if we go next? Well, there, there could be two possibilities, right? One is redund null, and the other one could be that it, it, it, it, it done as an exception. It, there is a runtime exception is the second. OK, it may return now, but it doesn't. It generates an exception, runtime exception, so your programme is gonna break. OK? So this is why we have to be very careful when using next. Because if we go next and the the iterator is past the end. We are done. Our programme is not going to run. OK. They can be used to walk through the elements of list but not those of queues. What are, what are cues? Are cues collections? Yes, queues are collections. So we can iterate over any collection, so this is, this is false. They can be, uh, they expose the underlying data storage layout of the collection being traversed. Do they expose the details? When calling has next and when calling next, are you using the links? The links, the next and the previous and all this stuff. Or are you accessing the array? So do you still see, do you still think that they are exposing the details of how it is internally stored? No, they don't. Indeed this is the main, the main motivation for iterators. Because they let you walk through the collection. Without knowing what you are traversing. The haz next method can always be called no matter the position of the iterator. True. And the next method can always be called no matter the position of the iterator. Oh, no, no. OK. So if you need to frequently add or remove elements in the middle of a collection, which collection class implementation would you use? See the list. Why? It's very efficient to add elements and why not not a redex. Because you cannot add elements in the middle. That's it, right? I mean, you would be using something which is not fit for purpose, no? I mean, like, like trying to use a hammer with a computer, right? Well, you could do that, but it's not recommended. OK. If you need efficient random access to the elements of a collection using their index, which collection class implementation would you use? A realist And why not a race? It's a mistake Maybe I, I, I wrote it too fast without thinking too much. There you go, there you go. A ray is not a collection. Well done. OK. Assume that The current position of a listed erator is that one, OK? Which would be the position of of the iterator and and the state of the list after we call listederator removed. Er Cannot tell Cannot tell because I didn't tell you whether I whether I call next or preview so it's undefined. Does it make sense? It's a tricky question. To catch you up. OK. OK, so let's move now to hash tables. OK, so this is one of the, let me do my best because for some people it's hard to understand. But hash tables is uh one of the most important data structures that we are gonna be talking about in this course and it's a very famous one in computing, OK? And it's the actually the fundamental uh principle in many data structures that the Java collections framework provides, OK? So the problem with a linked list. Right? Is that in order to in order to look for a particular element, and we don't know where it is located, right? It takes a while to find the element. Right? So imagine that I have a list. No matter whether it's an array list or a lean list, and I want to find whether a given element, not the index, a given element is present or is not present in the list. What you have to do to locate the list. Sorry, what do you have to do to locate the element in the list? I believe all the elements to be. You have to search Right? Until you find one which is equal and if you don't find it. You have to scan through the whole thing. OK. So has tables. Are they the structures that provide a very fast solution to that problem. OK? So it's gonna be a data structure that is going to help us finding an element in a collection. Like this, OK? Very fast. OK. And How does it work? It always computes hash codes, OK? So when you want to insert an element into the hash table or when you want to find whether an element is in a hash table, it will always compute the hash code. OK? What is the hash code of an object? It's an integer, right? It is an integer that is computed how it is computed. No That's a possible way of computing it, but at the end of the day it's a number that somehow represents the object, the state of the object, because it's compute from the fields of the object, right? If I change. The value of one of the fields of an object, do you think that the hash code is going to change or not? Yes, it changes, right? Here in the table, you have different hash codes, right? Do you see Le L E E L? They are very similar to each other, right? But they are different, so they are providing different hash codes, right? And do you think that we can have Two different objects with the same hash code or not? Yes, we can. Yes, we can. OK. So I see that you have studied. Well, that, that simplifies my job a lot. You cannot imagine how much. OK. Besides, there is a condition, right? A compatibility condition that we have to fulfil among the equals method and the hash code, right? And the compatibility condition is that if two elements are equal, if two objects are equal. With the notion of equality expressed in terms of the equals method, then the hash codes have to be equal, OK? The opposite is not true, right? But the implication the the Um, the implication, right, is from left to right, right? Not from right to left, OK? So in Java hash tables are implemented using an array of link lists, OK? So we have an array. The array has a fixed size, OK? Maybe we can have 16 elements, or we can have 32 elements, or we can have 64 elements. And in this array, in each of the entries, what I have is a linked list. The link lists are called the buckets. OK? The buckets. OK. The question is, OK, why and how do I leverage that structure in order to find the elements in a very fast way? Because this is what I promised to you, right? I promised to you that I will be able to deliver or to find the elements in a very fast way, right? Do you have an idea? Similar buckets. Yes, buckets. Yes. Yes, but this is a rough, right? A rough idea, which is in the right direction, but we need to define it more closely, right? Imagine that I have 16 elements in my array. OK, I have an object. I have to decide in which of the buckets I'm gonna put the object, right? And for, to this end, I compute the HA code. Right? But the has code is a, is a number that goes from It's a 32 bit integer, right? It's an in And it goes from 0. Up to 231 minus 1, right? And this number is approximately 2 billion, no? OK? I know that number that very well. I can tell you a story, but not now because we are running out of time. But if you are interested, OK, so But we only have 16 in the array. Or we may have 4, I don't know. We have to decide. So the question is, given a number that goes from 0 to 2 billion. How do we decide in which bucket are we gonna put? The the object Do you have an idea? And it has to be a deterministic criterion. It cannot be random, right? It has to be deterministic. Imagine that we have 4. Right? 444 buckets. So the array has 4012 and 3. How can you generate a number from 0 to 3? Out of a number that can be as large as 2 billion. That's a green novel. No, OK, integer division and the remainder of integer division. So if we take the remainder of the division. Of a large number. And 4 What are we gonna get? Sorry? The reminder, the reminder, right? I mean, in high school, right? Not in high school, in elementary school, right? You, you have A, no, divided by C, no? D, right? And A is equal to B multiplied by C plus D, right? And D is the reminder, right? This is the reminder. So if we take the integer division of A, a number being as large as 2 billion. Or 100 or 1000 or I don't know whatever and 4. What which are the possi B A is a last number, B is 4, which are the possible values of D. 0123. Come on. We have to go to elementary school again, 012 or 3, right? So if we take the hash code and we take the remainder of the entire division and the size of the array, we are gonna be getting an index which is between 0 and the size of the array minus 1. And this is the bucket where I'm gonna locate my object. But what can happen there? Are we, are we, are we done? What happens if two objects, different objects, have the same hash code? That can happen, no? What is gonna happen there? Interesting. I will go to the same bucket. That's called a hash collision. We can handle it, but what do we have to do? Do we just inject the object in the bucket or what, what do we have to do? Imagine that we are inserting. To reduce the collision and can multiply. I don't know. Well, that's another thing. We are, we are, we are, we are, that's talking about how to place the thing. I'm, I'm just saying. Imagine that they have an object. I call the I I compute the hash code. Then I take the reminder of the entire division of the hash code and the size of the array. Then I know the bucket in which I have to put the object, but in that bucket there are already objects, so the link list is not empty. What do you think I need to do in recall that this is a set that cannot be duplicates. But what do you think I have to do with the, with the bucket with the link list? Can I just blindly insert, insert the element? No, because if I do that, there might be a duplicate. What do you think I have to do? To search, to go through the linked list. Sequentially, next, next, next, next, right? And then if, if, if I'm not able to find it, then I inject it at the end of the bucket, right? If I'm able to find it. I tell the user, it's already there. I cannot insert it, right? And this is why a can return false because in some circumstances I may try to add the element and the element is not there, OK? Do you get the dynamics? Do you understand the dynamics? This is actually very important. OK? It is important that you understand what we are talking about, OK? And this is mostly what I have told, right? The question is Do you foresee any problem, any efficiency issue with this? So imagine the worst case scenario in which our array has dimension one, length one. What's going to happen? So I only have one bucket. What's gonna happen? I inject one element. I go to the bucket there. Another element, all the has codes are gonna go to the same bucket because there is only one bucket. So this is essentially a linked list. Right? If I increase the number of buckets, I have to be lucky. To be very efficient. Such that the hash codes are gonna be uniformly distributed across the array because it may happen that I'm so unlucky that all the elements that I'm introducing into the into the hash set collision into the same bucket. Right? Collide, collide into the same bucket. So it's a probabilistic thing. Depending on the distribution, right? Of the hash codes of the elements that I am introducing into the set. Right? I may get faster or lower performance. The question or the the the the power of this thing is that we have, we can choose the parameters of the hash table. We can choose how large The array is And we can choose another parameter which is called the load factor. The load factor is something that is gonna tell us whenever the buckets, whenever my array is full enough, I'm gonna be making larger. OK? So for example, there is a a parameter which is 0.75 by default in the Java collections framework. Which Whenever the array. Gets 75% full or larger, then I'm gonna reallocate it to make it larger. OK? And then I have to rehash everything, right? But the idea is that the experience, although this is. This is, this requires stochastic mathematics. A analysing the performance of this requires advanced arguments in stochastic mathematics, but to simplify things, right, at the end of the day, the idea is that on average, right, if you properly size your table. Of course, if you, if you, if you don't know what you are doing and you select that the size of the hash table is gonna be one, you are gonna screw it up. But if you select wisely these two parameters, the size of the array and the low factor, on average, when you have multiple accesses to the hash table, the cost of accessing is gonna be just as accessing a random random access. It's magic. I know it's magic, but this is what the stochastic analysis tells us is that these sort of tables are such that whenever we access in sequence, right, we have 11,000 or 10,000 access to the hash table. That, that's gonna happen in your programme because your programme executed like this, right? You're gonna be accessing all the people with the mobile phones, right? Try to access to your application, right? The table is gonna be, uh, all the time modified, right? So on average the stochastic analysis of this kind of data structures tells us that the cost of accessing the hash table on average is gonna be equivalent as the cost of random access. OK? So this is the beauty, this is the beauty of has tables, OK? OK, so the hasset. Is a set That implements the set interface and is based on the concept of a hash table, that's it. No more to say, OK? And an important note. That is pretty subtle, but it can actually happen in practise, is that What happens if you insert? An element In a hash table. And after inserting it, you modify that object. Well, what is going to happen if we modify the object is that the hash code is going to change. And if the, if the, if the hash code changes, Then it's not properly located. Right? Because it's not in the proper bucket. So if we modify the object and later we ask, is the object present, we are gonna get false, but it is there. So you have to be very careful when working with hashsets. Because if you insert an element and you modify it afterwards, then the headset the headset is no longer in a consistent state. And actually this is something that in other programming languages, you cannot do. For example, in Python, who has has programmed in Python before? Can you raise your hand? OK. Have you ever used a dictionary? Yes, in the dictionary, the keys of the dictionary can only be types, can only be of types which are immutable. So you cannot put, for example, a list. As the type of the of the key of the dictionary. That's because to avoid this problem. But in Java, you can, and you have to be very careful about that, OK? OK. And we also have the three set, OK? Remember that we have 2 different implementations of sets. One is the hash set, the other one is the tree set, and the tree set is a sorted set. So the tree set is gonna help us keep. An order of the elements in the set. And this is different from the priority queue because remember that in the priority queue we do not store the elements in order, we do not keep the elements inside, we just provide a mechanism to get the smallest element in a very fast way here we strictly. Store the elements in a sorted order again, according to a sorting criterion which is specified through comparable or comparator, right? And for it's good for you to know that internally, it uses a data structure which is called Red Black Tree, OK? So if you click there, you will see the Wikipedia page and you can see the data structure. We are not actually going to evaluate you on that, right? But it's for, for you it's good to know what is behind the things that you are using, right? This amounts to asking ourselves what we are doing, right? To be professionals that know what they are doing, right? And let me mention that you may imagine that on average inserting elements or accessing elements in a red-black tree is slightly uh uh uh slower than accessing in a high table, OK? So it has a penalty. Performance penalty. Keeping the elements sorted in the tree has a performance penalty, OK? But still, much faster. Much faster than um Has it, that's much faster than a red list or a link list, OK? OK, so let us leave it here. Uh, I still have to cover maps. Right, and the genetic algorithms, I will record the lecture, the the rest part, the part of the lecture which is missing, but at the end of the day, maps is just a collection that stores, not collection, it's something, it's an object that stores key value pairs like a dictionary in Python. And we have different kind of maps, but we have hash maps and tree maps, and they are based on the same concepts, the concept of a hash table and the concept of a red-black tree for the keys. And then we, we will talk a little bit about genetic algorithms, OK? So thank you very much for your attention and see you next week. Yes, I'm interested in about, uh, change the object after you said uh. Hassette, yeah, wait a, wait a moment because I need to stop the recording. Sorry, sorry. Yeah sorry about that because the thing is that if I don't do this.
